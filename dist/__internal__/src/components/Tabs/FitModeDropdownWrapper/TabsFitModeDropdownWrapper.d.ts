import './TabsFitModeDropdownWrapper.css';
import React from 'react';
import { TabsFitModeWrapperProps } from '../helpers';
export declare const TabsFitModeDropdownWrapper: <ITEM>({ items, tabsDimensions, tabRefs, getLabel, getChecked, renderItem, renderItemsList, }: TabsFitModeWrapperProps<ITEM>) => React.ReactElement | null;
