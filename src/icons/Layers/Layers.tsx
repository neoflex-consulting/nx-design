import { createIcon } from '../_createIcon/createIcon';

import LayersSizeAll from './Layers_size_all';

export const Layers = createIcon({
  l: LayersSizeAll,
  m: LayersSizeAll,
  s: LayersSizeAll,
  xs: LayersSizeAll,
  name: 'Layers',
});
