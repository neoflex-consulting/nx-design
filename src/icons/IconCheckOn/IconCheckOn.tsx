import { createIcon } from '../_createIcon/createIcon';

import IconCheckOnSizeAll from './IconCheckOn_size_all';

export const IconCheckOn = createIcon({
  l: IconCheckOnSizeAll,
  m: IconCheckOnSizeAll,
  s: IconCheckOnSizeAll,
  xs: IconCheckOnSizeAll,
  name: 'IconCheckOn',
});
