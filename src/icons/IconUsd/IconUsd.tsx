import { createIcon } from '../_createIcon/createIcon';

import IconUsdSizeAll from './IconUsd_size_all';

export const IconUsd = createIcon({
  l: IconUsdSizeAll,
  m: IconUsdSizeAll,
  s: IconUsdSizeAll,
  xs: IconUsdSizeAll,
  name: 'IconUsd',
});
