import { createIcon } from '../_createIcon/createIcon';

import LinkSizeAll from './Link_size_all';

export const Link = createIcon({
  l: LinkSizeAll,
  m: LinkSizeAll,
  s: LinkSizeAll,
  xs: LinkSizeAll,
  name: 'IconLink',
});
