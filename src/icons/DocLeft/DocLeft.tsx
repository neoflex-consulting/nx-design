import { createIcon } from '../_createIcon/createIcon';

import DocLeftSizeAll from './DocLeft_size_all';

export const DocLeft = createIcon({
  l: DocLeftSizeAll,
  m: DocLeftSizeAll,
  s: DocLeftSizeAll,
  xs: DocLeftSizeAll,
  name: 'IconDocLeft',
});
