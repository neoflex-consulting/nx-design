import { createIcon } from '../../../_createIcon/createIcon';

import PrintSizeAll from './Print_size_all';

export const Print = createIcon({
  m: PrintSizeAll,
  s: PrintSizeAll,
  xs: PrintSizeAll,
  name: 'Print',
});
