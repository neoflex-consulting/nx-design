import { createIcon } from '../../../_createIcon/createIcon';

import SertificatSizeAll from './Sertificat_size_all';

export const Sertificat = createIcon({
  m: SertificatSizeAll,
  s: SertificatSizeAll,
  xs: SertificatSizeAll,
  name: 'Sertificat',
});
