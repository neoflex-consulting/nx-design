import { createIcon } from '../../../_createIcon/createIcon';

import TabletSizeAll from './Tablet_size_all';

export const Tablet = createIcon({
  m: TabletSizeAll,
  s: TabletSizeAll,
  xs: TabletSizeAll,
  name: 'Tablet',
});
