import { createIcon } from '../../../_createIcon/createIcon';

import StopFilledSizeAll from './StopFilled_size_all';

export const StopFilled = createIcon({
  m: StopFilledSizeAll,
  s: StopFilledSizeAll,
  xs: StopFilledSizeAll,
  name: 'StopFilled',
});
