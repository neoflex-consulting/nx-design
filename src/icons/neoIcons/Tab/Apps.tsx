import { createIcon } from '../../../_createIcon/createIcon';

import TabSizeAll from './Tab_size_all';

export const Tab = createIcon({
  m: TabSizeAll,
  s: TabSizeAll,
  xs: TabSizeAll,
  name: 'Tab',
});
