import { createIcon } from '../../../_createIcon/createIcon';

import UserSizeAll from './User_size_all';

export const User = createIcon({
  m: UserSizeAll,
  s: UserSizeAll,
  xs: UserSizeAll,
  name: 'User',
});
