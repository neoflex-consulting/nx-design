import { createIcon } from '../../../_createIcon/createIcon';

import TimerSizeAll from './Timer_size_all';

export const Timer = createIcon({
  m: TimerSizeAll,
  s: TimerSizeAll,
  xs: TimerSizeAll,
  name: 'Timer',
});
