import { createIcon } from '../../../_createIcon/createIcon';

import QuestionSizeAll from './Question_size_all';

export const Question = createIcon({
  m: QuestionSizeAll,
  s: QuestionSizeAll,
  xs: QuestionSizeAll,
  name: 'Question',
});
