import { createIcon } from '../../../createIcon/createIcon';

import FolderMinusSizeAll from './FolderMinus_size_all';

export const FolderMinus = createIcon({
  m: FolderMinusSizeAll,
  s: FolderMinusSizeAll,
  xs: FolderMinusSizeAll,
  name: 'FolderMinus',
});
