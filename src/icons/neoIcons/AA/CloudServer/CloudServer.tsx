import { createIcon } from '../../../createIcon/createIcon';

import CloudServerSizeAll from './CloudServer_size_all';

export const CloudServer = createIcon({
  m: CloudServerSizeAll,
  s: CloudServerSizeAll,
  xs: CloudServerSizeAll,
  name: 'CloudServer',
});
