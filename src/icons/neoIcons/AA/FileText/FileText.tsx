import { createIcon } from '../../../_createIcon/createIcon';

import FileTextSizeAll from './FileText_size_all';

export const FileText = createIcon({
  m: FileTextSizeAll,
  s: FileTextSizeAll,
  xs: FileTextSizeAll,
  name: 'FileText',
});
