import { createIcon } from '../../../_createIcon/createIcon';

import BuildingSizeAll from './Building_size_all';

export const Building = createIcon({
  m: BuildingSizeAll,
  s: BuildingSizeAll,
  xs: BuildingSizeAll,
  name: 'Building',
});
