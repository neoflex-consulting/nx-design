import { createIcon } from '../../../createIcon/createIcon';

import FolderPlusSizeAll from './FolderPlus_size_all';

export const FolderPlus = createIcon({
  m: FolderPlusSizeAll,
  s: FolderPlusSizeAll,
  xs: FolderPlusSizeAll,
  name: 'FolderPlus',
});
