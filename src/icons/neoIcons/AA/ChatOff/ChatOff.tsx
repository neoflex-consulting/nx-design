import { createIcon } from '../../../createIcon/createIcon';

import ChatOffSizeAll from './ChatOff_size_all';

export const ChatOff = createIcon({
  m: ChatOffSizeAll,
  s: ChatOffSizeAll,
  xs: ChatOffSizeAll,
  name: 'ChatOff',
});
