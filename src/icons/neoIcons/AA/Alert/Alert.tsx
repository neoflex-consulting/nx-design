import { createIcon } from '../../../createIcon/createIcon';

import AlertSizeAll from './Alert_size_all';

export const Alert = createIcon({
  m: AlertSizeAll,
  s: AlertSizeAll,
  xs: AlertSizeAll,
  name: 'Alert',
});
