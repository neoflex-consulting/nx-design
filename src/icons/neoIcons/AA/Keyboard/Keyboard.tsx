import { createIcon } from '../../../_createIcon/createIcon';

import KeyboardSizeAll from './Keyboard_size_all';

export const Keyboard = createIcon({
  m: KeyboardSizeAll,
  s: KeyboardSizeAll,
  xs: KeyboardSizeAll,
  name: 'Keyboard',
});
