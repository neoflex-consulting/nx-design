import { createIcon } from '../../../_createIcon/createIcon';

import NotepadSizeAll from './Notepad_size_all';

export const Notepad = createIcon({
  m: NotepadSizeAll,
  s: NotepadSizeAll,
  xs: NotepadSizeAll,
  name: 'Notepad',
});
