import { createIcon } from '../../../_createIcon/createIcon';

import CookiesSizeAll from './Cookies_size_all';

export const Cookies = createIcon({
  m: CookiesSizeAll,
  s: CookiesSizeAll,
  xs: CookiesSizeAll,
  name: 'Cookies',
});
