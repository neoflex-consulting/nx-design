import { createIcon } from '../../../createIcon/createIcon';

import FileDownloadSizeAll from './FileDownload_size_all';

export const FileDownload = createIcon({
  m: FileDownloadSizeAll,
  s: FileDownloadSizeAll,
  xs: FileDownloadSizeAll,
  name: 'FileDownload',
});
