import { createIcon } from '../../../_createIcon/createIcon';

import ListCheckSizeAll from './ListCheck_size_all';

export const ListCheck = createIcon({
  m: ListCheckSizeAll,
  s: ListCheckSizeAll,
  xs: ListCheckSizeAll,
  name: 'ListCheck',
});
