import { createIcon } from '../../../_createIcon/createIcon';

import ColorSizeAll from './Color_size_all';

export const Color = createIcon({
  m: ColorSizeAll,
  s: ColorSizeAll,
  xs: ColorSizeAll,
  name: 'Color',
});
