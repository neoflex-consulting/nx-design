import { createIcon } from '../../../_createIcon/createIcon';

import LaptopSizeAll from './Laptop_size_all';

export const Laptop = createIcon({
  m: LaptopSizeAll,
  s: LaptopSizeAll,
  xs: LaptopSizeAll,
  name: 'Laptop',
});
