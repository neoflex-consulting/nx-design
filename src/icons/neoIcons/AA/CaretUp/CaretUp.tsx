import { createIcon } from '../../../createIcon/createIcon';

import CaretUpSizeAll from './CaretUp_size_all';

export const CaretUp = createIcon({
  m: CaretUpSizeAll,
  s: CaretUpSizeAll,
  xs: CaretUpSizeAll,
  name: 'CaretUp',
});
