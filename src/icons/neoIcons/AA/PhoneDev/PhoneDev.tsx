import { createIcon } from '../../../_createIcon/createIcon';

import PhoneDevSizeAll from './PhoneDev_size_all';

export const PhoneDev = createIcon({
  m: PhoneDevSizeAll,
  s: PhoneDevSizeAll,
  xs: PhoneDevSizeAll,
  name: 'PhoneDev',
});
