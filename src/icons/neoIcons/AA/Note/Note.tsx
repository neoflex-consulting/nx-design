import { createIcon } from '../../../_createIcon/createIcon';

import NoteSizeAll from './Note_size_all';

export const Note = createIcon({
  m: NoteSizeAll,
  s: NoteSizeAll,
  xs: NoteSizeAll,
  name: 'Note',
});
