import { createIcon } from '../../../_createIcon/createIcon';

import ArrowSwapSizeAll from './ArrowSwap_size_all';

export const ArrowSwap = createIcon({
  m: ArrowSwapSizeAll,
  s: ArrowSwapSizeAll,
  xs: ArrowSwapSizeAll,
  name: 'ArrowSwap',
});
