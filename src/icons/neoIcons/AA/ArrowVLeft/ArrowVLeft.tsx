import { createIcon } from '../../../_createIcon/createIcon';

import ArrowVLeftSizeAll from './ArrowVLeft_size_all';

export const ArrowVLeft = createIcon({
  m: ArrowVLeftSizeAll,
  s: ArrowVLeftSizeAll,
  xs: ArrowVLeftSizeAll,
  name: 'ArrowVLeft',
});
