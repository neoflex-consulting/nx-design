import { createIcon } from '../../../createIcon/createIcon';

import BlockSizeAll from './Block_size_all';

export const Block = createIcon({
  m: BlockSizeAll,
  s: BlockSizeAll,
  xs: BlockSizeAll,
  name: 'Block',
});
