import { createIcon } from '../../../_createIcon/createIcon';

import BaseSizeAll from './Base_size_all';

export const Base = createIcon({
  m: BaseSizeAll,
  s: BaseSizeAll,
  xs: BaseSizeAll,
  name: 'Base',
});
