import { createIcon } from '../../../_createIcon/createIcon';

import MinusSquareSizeAll from './MinusSquare_size_all';

export const MinusSquare = createIcon({
  m: MinusSquareSizeAll,
  s: MinusSquareSizeAll,
  xs: MinusSquareSizeAll,
  name: 'MinusSquare',
});
