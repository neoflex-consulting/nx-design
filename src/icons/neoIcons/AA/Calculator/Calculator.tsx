import { createIcon } from '../../../_createIcon/createIcon';

import CalculatorSizeAll from './Calculator_size_all';

export const Calculator = createIcon({
  m: CalculatorSizeAll,
  s: CalculatorSizeAll,
  xs: CalculatorSizeAll,
  name: 'Calculator',
});
