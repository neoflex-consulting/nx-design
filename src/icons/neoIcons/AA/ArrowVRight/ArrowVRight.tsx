import { createIcon } from '../../../createIcon/createIcon';

import ArrowVRightSizeAll from './ArrowVRight_size_all';

export const ArrowVRight = createIcon({
  m: ArrowVRightSizeAll,
  s: ArrowVRightSizeAll,
  xs: ArrowVRightSizeAll,
  name: 'ArrowVRight',
});
