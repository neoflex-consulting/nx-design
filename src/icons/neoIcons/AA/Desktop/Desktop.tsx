import { createIcon } from '../../../createIcon/createIcon';

import DesktopSizeAll from './Desktop_size_all';

export const Desktop = createIcon({
  m: DesktopSizeAll,
  s: DesktopSizeAll,
  xs: DesktopSizeAll,
  name: 'Desktop',
});
