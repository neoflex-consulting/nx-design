import { createIcon } from '../../../_createIcon/createIcon';

import LockOffSizeAll from './LockOff_size_all';

export const LockOff = createIcon({
  m: LockOffSizeAll,
  s: LockOffSizeAll,
  xs: LockOffSizeAll,
  name: 'LockOff',
});
