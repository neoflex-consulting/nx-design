import { createIcon } from '../../../createIcon/createIcon';

import DarkThemeSizeAll from './DarkTheme_size_all';

export const DarkTheme = createIcon({
  m: DarkThemeSizeAll,
  s: DarkThemeSizeAll,
  xs: DarkThemeSizeAll,
  name: 'DarkTheme',
});
