import { createIcon } from '../../../createIcon/createIcon';

import DisplayOnlySizeAll from './DisplayOnly_size_all';

export const DisplayOnly = createIcon({
  m: DisplayOnlySizeAll,
  s: DisplayOnlySizeAll,
  xs: DisplayOnlySizeAll,
  name: 'DisplayOnly',
});
