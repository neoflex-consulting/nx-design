import { createIcon } from '../../../createIcon/createIcon';

import EditorRegionSizeAll from './EditorRegion_size_all';

export const EditorRegion = createIcon({
  m: EditorRegionSizeAll,
  s: EditorRegionSizeAll,
  xs: EditorRegionSizeAll,
  name: 'EditorRegion',
});
