import { createIcon } from '../../../createIcon/createIcon';

import FolderDeleteSizeAll from './FolderDelete_size_all';

export const FolderDelete = createIcon({
  m: FolderDeleteSizeAll,
  s: FolderDeleteSizeAll,
  xs: FolderDeleteSizeAll,
  name: 'FolderDelete',
});
