import { createIcon } from '../../../createIcon/createIcon';

import DeleteSizeAll from './Delete_size_all';

export const Delete = createIcon({
  m: DeleteSizeAll,
  s: DeleteSizeAll,
  xs: DeleteSizeAll,
  name: 'Delete',
});
