import { createIcon } from '../../../_createIcon/createIcon';

import ContactSizeAll from './Contact_size_all';

export const Contact = createIcon({
  m: ContactSizeAll,
  s: ContactSizeAll,
  xs: ContactSizeAll,
  name: 'Contact',
});
