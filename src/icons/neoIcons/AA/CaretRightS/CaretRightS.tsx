import { createIcon } from '../../../createIcon/createIcon';

import CaretRightSSizeAll from './CaretRightS_size_all';

export const CaretRightS = createIcon({
  m: CaretRightSSizeAll,
  s: CaretRightSSizeAll,
  xs: CaretRightSSizeAll,
  name: 'CaretRightS',
});
