import { createIcon } from '../../../createIcon/createIcon';

import BookWarningSizeAll from './BookWarning_size_all';

export const BookWarning = createIcon({
  m: BookWarningSizeAll,
  s: BookWarningSizeAll,
  xs: BookWarningSizeAll,
  name: 'BookWarning',
});
