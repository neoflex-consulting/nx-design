import { createIcon } from '../../../_createIcon/createIcon';

import ArrowLeftSizeAll from './ArrowLeft_size_all';

export const ArrowLeft = createIcon({
  m: ArrowLeftSizeAll,
  s: ArrowLeftSizeAll,
  xs: ArrowLeftSizeAll,
  name: 'ArrowLeft',
});
