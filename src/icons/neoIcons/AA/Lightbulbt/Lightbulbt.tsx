import { createIcon } from '../../../_createIcon/createIcon';

import LightbulbtSizeAll from './Lightbulbt_size_all';

export const Lightbulbt = createIcon({
  m: LightbulbtSizeAll,
  s: LightbulbtSizeAll,
  xs: LightbulbtSizeAll,
  name: 'Lightbulbt',
});
