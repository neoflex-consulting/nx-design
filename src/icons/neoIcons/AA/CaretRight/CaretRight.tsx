import { createIcon } from '../../../_createIcon/createIcon';

import CaretRightSizeAll from './CaretRight_size_all';

export const CaretRight = createIcon({
  m: CaretRightSizeAll,
  s: CaretRightSizeAll,
  xs: CaretRightSizeAll,
  name: 'CaretRight',
});
