import { createIcon } from '../../../createIcon/createIcon';

import ArrowRightSizeAll from './ArrowRight_size_all';

export const ArrowRight = createIcon({
  m: ArrowRightSizeAll,
  s: ArrowRightSizeAll,
  xs: ArrowRightSizeAll,
  name: 'ArrowRight',
});
