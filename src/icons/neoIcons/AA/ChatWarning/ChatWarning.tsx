import { createIcon } from '../../../createIcon/createIcon';

import ChatWarningSizeAll from './ChatWarning_size_all';

export const ChatWarning = createIcon({
  m: ChatWarningSizeAll,
  s: ChatWarningSizeAll,
  xs: ChatWarningSizeAll,
  name: 'ChatWarning',
});
