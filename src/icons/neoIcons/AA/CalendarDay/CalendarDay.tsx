import { createIcon } from '../../../createIcon/createIcon';

import CalendarDaySizeAll from './CalendarDay_size_all';

export const CalendarDay = createIcon({
  m: CalendarDaySizeAll,
  s: CalendarDaySizeAll,
  xs: CalendarDaySizeAll,
  name: 'CalendarDay',
});
