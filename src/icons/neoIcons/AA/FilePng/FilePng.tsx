import { createIcon } from '../../../_createIcon/createIcon';

import FilePngSizeAll from './FilePng_size_all';

export const FilePng = createIcon({
  m: FilePngSizeAll,
  s: FilePngSizeAll,
  xs: FilePngSizeAll,
  name: 'FilePng',
});
