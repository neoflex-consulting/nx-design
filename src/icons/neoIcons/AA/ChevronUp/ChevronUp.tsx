import { createIcon } from '../../../_createIcon/createIcon';

import ChevronUpSizeAll from './ChevronUp_size_all';

export const ChevronUp = createIcon({
  m: ChevronUpSizeAll,
  s: ChevronUpSizeAll,
  xs: ChevronUpSizeAll,
  name: 'ChevronUp',
});
