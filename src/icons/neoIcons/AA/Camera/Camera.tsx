import { createIcon } from '../../../createIcon/createIcon';

import CameraSizeAll from './Camera_size_all';

export const Camera = createIcon({
  m: CameraSizeAll,
  s: CameraSizeAll,
  xs: CameraSizeAll,
  name: 'Camera',
});
