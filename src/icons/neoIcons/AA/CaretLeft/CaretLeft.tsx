import { createIcon } from '../../../_createIcon/createIcon';

import CaretLeftSizeAll from './CaretLeft_size_all';

export const CaretLeft = createIcon({
  m: CaretLeftSizeAll,
  s: CaretLeftSizeAll,
  xs: CaretLeftSizeAll,
  name: 'CaretLeft',
});
