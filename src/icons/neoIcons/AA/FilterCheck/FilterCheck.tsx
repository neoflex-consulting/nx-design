import { createIcon } from '../../../_createIcon/createIcon';

import FilterCheckSizeAll from './FilterCheck_size_all';

export const FilterCheck = createIcon({
  m: FilterCheckSizeAll,
  s: FilterCheckSizeAll,
  xs: FilterCheckSizeAll,
  name: 'FilterCheck',
});
