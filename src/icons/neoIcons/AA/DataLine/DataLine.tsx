import { createIcon } from '../../../createIcon/createIcon';

import DataLineSizeAll from './DataLine_size_all';

export const DataLine = createIcon({
  m: DataLineSizeAll,
  s: DataLineSizeAll,
  xs: DataLineSizeAll,
  name: 'DataLine',
});
