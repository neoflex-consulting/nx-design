import { createIcon } from '../../../createIcon/createIcon';

import DocumentSizeAll from './Document_size_all';

export const Document = createIcon({
  m: DocumentSizeAll,
  s: DocumentSizeAll,
  xs: DocumentSizeAll,
  name: 'Document',
});
