import { createIcon } from '../../../_createIcon/createIcon';

import MapSizeAll from './Map_size_all';

export const Map = createIcon({
  m: MapSizeAll,
  s: MapSizeAll,
  xs: MapSizeAll,
  name: 'Map',
});
