import { createIcon } from '../../../createIcon/createIcon';

import BookFinanceSizeAll from './BookFinance_size_all';

export const BookFinance = createIcon({
  m: BookFinanceSizeAll,
  s: BookFinanceSizeAll,
  xs: BookFinanceSizeAll,
  name: 'BookFinance',
});
