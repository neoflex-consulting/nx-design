import { createIcon } from '../../../_createIcon/createIcon';

import ConnectSizeAll from './Connect_size_all';

export const Connect = createIcon({
  m: ConnectSizeAll,
  s: ConnectSizeAll,
  xs: ConnectSizeAll,
  name: 'Connect',
});
