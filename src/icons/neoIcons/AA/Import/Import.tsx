import { createIcon } from '../../../_createIcon/createIcon';

import ImportSizeAll from './Import_size_all';

export const Import = createIcon({
  m: ImportSizeAll,
  s: ImportSizeAll,
  xs: ImportSizeAll,
  name: 'Import',
});
