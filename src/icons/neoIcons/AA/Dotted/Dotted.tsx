import { createIcon } from '../../../_createIcon/createIcon';

import DottedSizeAll from './Dotted_size_all';

export const Dotted = createIcon({
  m: DottedSizeAll,
  s: DottedSizeAll,
  xs: DottedSizeAll,
  name: 'Dotted',
});
