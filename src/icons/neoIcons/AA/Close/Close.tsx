import { createIcon } from '../../../createIcon/createIcon';

import CloseSizeAll from './Close_size_all';

export const Close = createIcon({
  m: CloseSizeAll,
  s: CloseSizeAll,
  xs: CloseSizeAll,
  name: 'Close',
});
