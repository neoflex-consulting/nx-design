import { createIcon } from '../../../createIcon/createIcon';

import DragSizeAll from './Drag_size_all';

export const Drag = createIcon({
  m: DragSizeAll,
  s: DragSizeAll,
  xs: DragSizeAll,
  name: 'Drag',
});
