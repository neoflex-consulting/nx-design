import { createIcon } from '../../../_createIcon/createIcon';

import MoveDownSizeAll from './MoveDown_size_all';

export const MoveDown = createIcon({
  m: MoveDownSizeAll,
  s: MoveDownSizeAll,
  xs: MoveDownSizeAll,
  name: 'MoveDown',
});
