import { createIcon } from '../../../createIcon/createIcon';

import FlashSizeAll from './Flash_size_all';

export const Flash = createIcon({
  m: FlashSizeAll,
  s: FlashSizeAll,
  xs: FlashSizeAll,
  name: 'Flash',
});
