import { createIcon } from '../../../createIcon/createIcon';

import CalendarNuleSizeAll from './CalendarNule_size_all';

export const CalendarNule = createIcon({
  m: CalendarNuleSizeAll,
  s: CalendarNuleSizeAll,
  xs: CalendarNuleSizeAll,
  name: 'CalendarNule',
});
