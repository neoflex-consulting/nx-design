import { createIcon } from '../../../createIcon/createIcon';

import CircleOSizeAll from './CircleO_size_all';

export const CircleO = createIcon({
  m: CircleOSizeAll,
  s: CircleOSizeAll,
  xs: CircleOSizeAll,
  name: 'CircleO',
});
