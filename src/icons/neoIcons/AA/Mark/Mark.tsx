import { createIcon } from '../../../_createIcon/createIcon';

import MarkSizeAll from './Mark_size_all';

export const Mark = createIcon({
  m: MarkSizeAll,
  s: MarkSizeAll,
  xs: MarkSizeAll,
  name: 'Mark',
});
