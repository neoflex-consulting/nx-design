import { createIcon } from '../../../createIcon/createIcon';

import BugSizeAll from './Bug_size_all';

export const Bug = createIcon({
  m: BugSizeAll,
  s: BugSizeAll,
  xs: BugSizeAll,
  name: 'Bug',
});
