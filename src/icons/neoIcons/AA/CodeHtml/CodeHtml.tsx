import { createIcon } from '../../../createIcon/createIcon';

import CodeHtmlSizeAll from './CodeHtml_size_all';

export const CodeHtml = createIcon({
  m: CodeHtmlSizeAll,
  s: CodeHtmlSizeAll,
  xs: CodeHtmlSizeAll,
  name: 'CodeHtml',
});
