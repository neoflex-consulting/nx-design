import { createIcon } from '../../../createIcon/createIcon';

import FolderSearchSizeAll from './FolderSearch_size_all';

export const FolderSearch = createIcon({
  m: FolderSearchSizeAll,
  s: FolderSearchSizeAll,
  xs: FolderSearchSizeAll,
  name: 'FolderSearch',
});
