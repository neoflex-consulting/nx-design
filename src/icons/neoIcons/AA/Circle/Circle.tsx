import { createIcon } from '../../../createIcon/createIcon';

import CircleSizeAll from './Circle_size_all';

export const Circle = createIcon({
  m: CircleSizeAll,
  s: CircleSizeAll,
  xs: CircleSizeAll,
  name: 'Circle',
});
