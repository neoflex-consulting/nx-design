import { createIcon } from '../../../createIcon/createIcon';

import ChevronRightSizeAll from './ChevronRight_size_all';

export const ChevronRight = createIcon({
  m: ChevronRightSizeAll,
  s: ChevronRightSizeAll,
  xs: ChevronRightSizeAll,
  name: 'ChevronRight',
});
