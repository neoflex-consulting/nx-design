import { createIcon } from '../../../createIcon/createIcon';

import DividerSizeAll from './Divider_size_all';

export const Divider = createIcon({
  m: DividerSizeAll,
  s: DividerSizeAll,
  xs: DividerSizeAll,
  name: 'Divider',
});
