import { createIcon } from '../../../createIcon/createIcon';

import FileMinusSizeAll from './FileMinus_size_all';

export const FileMinus = createIcon({
  m: FileMinusSizeAll,
  s: FileMinusSizeAll,
  xs: FileMinusSizeAll,
  name: 'FileMinus',
});
