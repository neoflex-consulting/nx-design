import { createIcon } from '../../../_createIcon/createIcon';

import CommentChatSizeAll from './CommentChat_size_all';

export const CommentChat = createIcon({
  m: CommentChatSizeAll,
  s: CommentChatSizeAll,
  xs: CommentChatSizeAll,
  name: 'CommentChat',
});
