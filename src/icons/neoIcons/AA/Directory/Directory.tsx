import { createIcon } from '../../../createIcon/createIcon';

import DirectorySizeAll from './Directory_size_all';

export const Directory = createIcon({
  m: DirectorySizeAll,
  s: DirectorySizeAll,
  xs: DirectorySizeAll,
  name: 'Directory',
});
