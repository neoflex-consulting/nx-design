import { createIcon } from '../../../createIcon/createIcon';

import CubSizeAll from './Cub_size_all';

export const Cub = createIcon({
  m: CubSizeAll,
  s: CubSizeAll,
  xs: CubSizeAll,
  name: 'Cub',
});
