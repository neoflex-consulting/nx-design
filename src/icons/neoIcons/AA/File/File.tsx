import { createIcon } from '../../../createIcon/createIcon';

import FileSizeAll from './File_size_all';

export const File = createIcon({
  m: FileSizeAll,
  s: FileSizeAll,
  xs: FileSizeAll,
  name: 'File',
});
