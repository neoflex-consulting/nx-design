import { createIcon } from '../../../createIcon/createIcon';

import ArrowMoveSizeAll from './ArrowMove_size_all';

export const ArrowMove = createIcon({
  m: ArrowMoveSizeAll,
  s: ArrowMoveSizeAll,
  xs: ArrowMoveSizeAll,
  name: 'ArrowMove',
});
