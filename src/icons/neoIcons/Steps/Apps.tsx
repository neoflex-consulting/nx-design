import { createIcon } from '../../../_createIcon/createIcon';

import StepsSizeAll from './Steps_size_all';

export const Steps = createIcon({
  m: StepsSizeAll,
  s: StepsSizeAll,
  xs: StepsSizeAll,
  name: 'Steps',
});
