import { createIcon } from '../../../_createIcon/createIcon';

import PlusSCheckSizeAll from './PlusSCheck_size_all';

export const PlusSCheck = createIcon({
  m: PlusSCheckSizeAll,
  s: PlusSCheckSizeAll,
  xs: PlusSCheckSizeAll,
  name: 'PlusSCheck',
});
