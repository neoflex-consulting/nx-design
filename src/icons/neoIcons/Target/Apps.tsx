import { createIcon } from '../../../_createIcon/createIcon';

import TargetSizeAll from './Target_size_all';

export const Target = createIcon({
  m: TargetSizeAll,
  s: TargetSizeAll,
  xs: TargetSizeAll,
  name: 'Target',
});
