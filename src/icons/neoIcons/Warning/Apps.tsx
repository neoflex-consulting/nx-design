import { createIcon } from '../../../_createIcon/createIcon';

import WarningSizeAll from './Warning_size_all';

export const Warning = createIcon({
  m: WarningSizeAll,
  s: WarningSizeAll,
  xs: WarningSizeAll,
  name: 'Warning',
});
