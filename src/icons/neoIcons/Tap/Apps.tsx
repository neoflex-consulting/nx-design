import { createIcon } from '../../../_createIcon/createIcon';

import TapSizeAll from './Tap_size_all';

export const Tap = createIcon({
  m: TapSizeAll,
  s: TapSizeAll,
  xs: TapSizeAll,
  name: 'Tap',
});
