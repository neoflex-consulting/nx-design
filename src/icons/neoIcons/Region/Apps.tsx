import { createIcon } from '../../../_createIcon/createIcon';

import RegionSizeAll from './Region_size_all';

export const Region = createIcon({
  m: RegionSizeAll,
  s: RegionSizeAll,
  xs: RegionSizeAll,
  name: 'Region',
});
