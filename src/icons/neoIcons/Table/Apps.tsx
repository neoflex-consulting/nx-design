import { createIcon } from '../../../_createIcon/createIcon';

import TableSizeAll from './Table_size_all';

export const Table = createIcon({
  m: TableSizeAll,
  s: TableSizeAll,
  xs: TableSizeAll,
  name: 'Table',
});
