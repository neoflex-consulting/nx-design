import { createIcon } from '../../../_createIcon/createIcon';

import PlusSquareSizeAll from './PlusSquare_size_all';

export const PlusSquare = createIcon({
  m: PlusSquareSizeAll,
  s: PlusSquareSizeAll,
  xs: PlusSquareSizeAll,
  name: 'PlusSquare',
});
