import { createIcon } from '../_createIcon/createIcon';

import IconContactSizeAll from './IconContact_size_all';

export const IconContact = createIcon({
  l: IconContactSizeAll,
  m: IconContactSizeAll,
  s: IconContactSizeAll,
  xs: IconContactSizeAll,
  name: 'IconContact',
});
