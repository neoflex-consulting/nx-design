import { createIcon } from '../_createIcon/createIcon';

import ArrowDownSizeAll from './ArrowDown_size_all';

export const ArrowDown = createIcon({
  l: ArrowDownSizeAll,
  m: ArrowDownSizeAll,
  s: ArrowDownSizeAll,
  xs: ArrowDownSizeAll,
  name: 'ArrowDown'
});
