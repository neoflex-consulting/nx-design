import { createIcon } from '../_createIcon/createIcon';

import SettingsSizeAll from './Settings_size_all';

export const Settings = createIcon({
  m: SettingsSizeAll,
  s: SettingsSizeAll,
  xs: SettingsSizeAll,
  name: 'Settings',
});
