import { createIcon } from '../_createIcon/createIcon';

import FileSearchSizeAll from './FileSearch_size_all';

export const FileSearch = createIcon({
  l: FileSearchSizeAll,
  m: FileSearchSizeAll,
  s: FileSearchSizeAll,
  xs: FileSearchSizeAll,
  name: 'IconFileSearch',
});
