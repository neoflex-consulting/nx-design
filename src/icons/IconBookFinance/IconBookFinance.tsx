import { createIcon } from '../_createIcon/createIcon';

import IconBookFinanceSizeAll from './IconBookFinance_size_all';

export const IconBookFinance = createIcon({
  l: IconBookFinanceSizeAll,
  m: IconBookFinanceSizeAll,
  s: IconBookFinanceSizeAll,
  xs: IconBookFinanceSizeAll,
  name: 'IconBookFinance',
});
