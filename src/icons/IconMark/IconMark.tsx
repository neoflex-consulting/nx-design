import { createIcon } from '../_createIcon/createIcon';

import IconMarkSizeAll from './IconMark_size_all';

export const IconMark = createIcon({
  l: IconMarkSizeAll,
  m: IconMarkSizeAll,
  s: IconMarkSizeAll,
  xs: IconMarkSizeAll,
  name: 'IconMark',
});
