import { createIcon } from '../_createIcon/createIcon';

import CheckSizeAll from './Check_size_all';

export const Check = createIcon({
  l: CheckSizeAll,
  m: CheckSizeAll,
  s: CheckSizeAll,
  xs: CheckSizeAll,
  name: 'IconCheck',
});
