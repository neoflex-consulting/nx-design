import { createIcon } from '../_createIcon/createIcon';

import IconAppsSizeAll from './IconApps_size_all';

export const IconApps = createIcon({
  l: IconAppsSizeAll,
  m: IconAppsSizeAll,
  s: IconAppsSizeAll,
  xs: IconAppsSizeAll,
  name: 'IconApps'
});
