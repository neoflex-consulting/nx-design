import { createIcon } from '../_createIcon/createIcon';

import FaxSizeAll from './Fax_size_all';

export const Fax = createIcon({
  l: FaxSizeAll,
  m: FaxSizeAll,
  s: FaxSizeAll,
  xs: FaxSizeAll,
  name: 'IconFax',
});
