import { createIcon } from '../_createIcon/createIcon';

import IconCaretLeftSizeAll from './IconCaretLeft_size_all';

export const IconCaretLeft = createIcon({
  l: IconCaretLeftSizeAll,
  m: IconCaretLeftSizeAll,
  s: IconCaretLeftSizeAll,
  xs: IconCaretLeftSizeAll,
  name: 'IconCaretLeft',
});
