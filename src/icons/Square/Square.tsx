import { createIcon } from '../_createIcon/createIcon';

import SquareSizeAll from './Square_size_all';

export const Square = createIcon({
  l: SquareSizeAll,
  m: SquareSizeAll,
  s: SquareSizeAll,
  xs: SquareSizeAll,
  name: 'Square',
});
