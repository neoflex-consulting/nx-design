import { createIcon } from '../_createIcon/createIcon';

import FullScreenSizeAll from './FullScreen_size_all';

export const FullScreen = createIcon({
  l: FullScreenSizeAll,
  m: FullScreenSizeAll,
  s: FullScreenSizeAll,
  xs: FullScreenSizeAll,
  name: 'IconFullScreen',
});
