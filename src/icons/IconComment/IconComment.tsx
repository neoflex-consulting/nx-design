import { createIcon } from '../_createIcon/createIcon';

import IconCommentSizeAll from './IconComment_size_all';

export const IconComment = createIcon({
  l: IconCommentSizeAll,
  m: IconCommentSizeAll,
  s: IconCommentSizeAll,
  xs: IconCommentSizeAll,
  name: 'IconComment',
});
