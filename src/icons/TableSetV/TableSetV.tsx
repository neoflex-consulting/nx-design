import { createIcon } from '../_createIcon/createIcon';

import TableSetVSizeAll from './TableSetV_size_all';

export const TableSetV = createIcon({
  m: TableSetVSizeAll,
  s: TableSetVSizeAll,
  xs: TableSetVSizeAll,
  name: 'TableSetV',
});
