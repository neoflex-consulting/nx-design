import { createIcon } from '../_createIcon/createIcon';

import LocationSizeAll from './Location_size_all';

export const Location = createIcon({
  l: LocationSizeAll,
  m: LocationSizeAll,
  s: LocationSizeAll,
  xs: LocationSizeAll,
  name: 'Location',
});
