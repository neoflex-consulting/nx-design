import { createIcon } from '../_createIcon/createIcon';

import IconKeyboardSizeAll from './IconKeyboard_size_all';

export const IconKeyboard = createIcon({
  l: IconKeyboardSizeAll,
  m: IconKeyboardSizeAll,
  s: IconKeyboardSizeAll,
  xs: IconKeyboardSizeAll,
  name: 'IconKeyboard',
});
