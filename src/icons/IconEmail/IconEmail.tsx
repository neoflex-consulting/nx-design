import { createIcon } from '../_createIcon/createIcon';

import IconEmailSizeAll from './IconEmail_size_all';

export const IconEmail = createIcon({
  l: IconEmailSizeAll,
  m: IconEmailSizeAll,
  s: IconEmailSizeAll,
  xs: IconEmailSizeAll,
  name: 'IconEmail',
});
