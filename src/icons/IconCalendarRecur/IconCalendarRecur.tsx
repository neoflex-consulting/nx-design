import { createIcon } from '../_createIcon/createIcon';

import IconCalendarRecurSizeAll from './IconCalendarRecur_size_all';

export const IconCalendarRecur = createIcon({
  l: IconCalendarRecurSizeAll,
  m: IconCalendarRecurSizeAll,
  s: IconCalendarRecurSizeAll,
  xs: IconCalendarRecurSizeAll,
  name: 'IconCalendarRecur',
});
