import { createIcon } from '../_createIcon/createIcon';

import IconSpeedSizeAll from './IconSpeed_size_all';

export const IconSpeed = createIcon({
  l: IconSpeedSizeAll,
  m: IconSpeedSizeAll,
  s: IconSpeedSizeAll,
  xs: IconSpeedSizeAll,
  name: 'IconSpeed',
});
