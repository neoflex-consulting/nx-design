import { createIcon } from '../_createIcon/createIcon';

import IconSignOutSizeAll from './IconSignOut_size_all';

export const IconSignOut = createIcon({
  l: IconSignOutSizeAll,
  m: IconSignOutSizeAll,
  s: IconSignOutSizeAll,
  xs: IconSignOutSizeAll,
  name: 'IconSignOut',
});
