import { createIcon } from '../_createIcon/createIcon';

import IconActionMenuSizeAll from './IconActionMenu_size_all';

export const IconActionMenu = createIcon({
  l: IconActionMenuSizeAll,
  m: IconActionMenuSizeAll,
  s: IconActionMenuSizeAll,
  xs: IconActionMenuSizeAll,
  name: 'IconActionMenu'
});
