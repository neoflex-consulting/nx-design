import { createIcon } from '../_createIcon/createIcon';

import PlusSizeAll from './Plus_size_all';

export const Plus = createIcon({
  l: PlusSizeAll,
  m: PlusSizeAll,
  s: PlusSizeAll,
  xs: PlusSizeAll,
  name: 'IconPlus',
});
