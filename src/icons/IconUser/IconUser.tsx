import { createIcon } from '../_createIcon/createIcon';

import IconUserSizeAll from './IconUser_size_all';

export const IconUser = createIcon({
  l: IconUserSizeAll,
  m: IconUserSizeAll,
  s: IconUserSizeAll,
  xs: IconUserSizeAll,
  name: 'IconUser',
});
