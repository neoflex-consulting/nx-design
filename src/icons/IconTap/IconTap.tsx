import { createIcon } from '../_createIcon/createIcon';

import IconTapSizeAll from './IconTap_size_all';

export const IconTap = createIcon({
  l: IconTapSizeAll,
  m: IconTapSizeAll,
  s: IconTapSizeAll,
  xs: IconTapSizeAll,
  name: 'IconTap',
});
