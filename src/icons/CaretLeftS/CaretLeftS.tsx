import { createIcon } from '../_createIcon/createIcon';

import CaretLeftSSizeAll from './CaretLeftS_size_all';

export const CaretLeftS = createIcon({
  l: CaretLeftSSizeAll,
  m: CaretLeftSSizeAll,
  s: CaretLeftSSizeAll,
  xs: CaretLeftSSizeAll,
  name: 'IconCaretLeftS',
});
