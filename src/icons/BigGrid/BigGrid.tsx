import { createIcon } from '../_createIcon/createIcon';

import BigGridSizeAll from './BigGrid_size_all';

export const BigGrid = createIcon({
  l: BigGridSizeAll,
  m: BigGridSizeAll,
  s: BigGridSizeAll,
  xs: BigGridSizeAll,
  name: 'IconBigGrid',
});
