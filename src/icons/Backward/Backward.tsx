import { createIcon } from '../_createIcon/createIcon';

import BackwardSizeAll from './Backward_size_all';

export const Backward = createIcon({
  l: BackwardSizeAll,
  m: BackwardSizeAll,
  s: BackwardSizeAll,
  xs: BackwardSizeAll,
  name: 'IconBackward',
});
