import { createIcon } from '../_createIcon/createIcon';

import IconUploadSizeAll from './IconUpload_size_all';

export const IconUpload = createIcon({
  l: IconUploadSizeAll,
  m: IconUploadSizeAll,
  s: IconUploadSizeAll,
  xs: IconUploadSizeAll,
  name: 'IconUpload',
});
