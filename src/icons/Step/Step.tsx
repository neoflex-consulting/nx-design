import { createIcon } from '../_createIcon/createIcon';

import StepSizeAll from './Step_size_all';

export const Step = createIcon({
  l: StepSizeAll,
  m: StepSizeAll,
  s: StepSizeAll,
  xs: StepSizeAll,
  name: 'IconStep',
});
