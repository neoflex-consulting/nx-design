import { createIcon } from '../_createIcon/createIcon';

import ControlSizeAll from './Control_size_all';

export const Control = createIcon({
  l: ControlSizeAll,
  m: ControlSizeAll,
  s: ControlSizeAll,
  xs: ControlSizeAll,
  name: 'IconControl',
});
