import { createIcon } from '../_createIcon/createIcon';

import LineChartSizeAll from './LineChart_size_all';

export const LineChart = createIcon({
  m: LineChartSizeAll,
  s: LineChartSizeAll,
  xs: LineChartSizeAll,
  name: 'LineChart',
});
