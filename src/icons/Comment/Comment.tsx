import { createIcon } from '../_createIcon/createIcon';

import CommentSizeAll from './Comment_size_all';

export const Comment = createIcon({
  l: CommentSizeAll,
  m: CommentSizeAll,
  s: CommentSizeAll,
  xs: CommentSizeAll,
  name: 'IconComment',
});
