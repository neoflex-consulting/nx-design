import { createIcon } from '../_createIcon/createIcon';

import IconBaseServerSizeAll from './IconBaseServer_size_all';

export const IconBaseServer = createIcon({
  l: IconBaseServerSizeAll,
  m: IconBaseServerSizeAll,
  s: IconBaseServerSizeAll,
  xs: IconBaseServerSizeAll,
  name: 'IconBaseServer',
});
