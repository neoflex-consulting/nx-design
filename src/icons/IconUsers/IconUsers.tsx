import { createIcon } from '../_createIcon/createIcon';

import IconUsersSizeAll from './IconUsers_size_all';

export const IconUsers = createIcon({
  l: IconUsersSizeAll,
  m: IconUsersSizeAll,
  s: IconUsersSizeAll,
  xs: IconUsersSizeAll,
  name: 'IconUsers',
});
