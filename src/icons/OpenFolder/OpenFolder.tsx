import { createIcon } from '../_createIcon/createIcon';

import OpenFolderSizeAll from './OpenFolder_size_all';

export const OpenFolder = createIcon({
  m: OpenFolderSizeAll,
  s: OpenFolderSizeAll,
  xs: OpenFolderSizeAll,
  name: 'OpenFolder',
});
