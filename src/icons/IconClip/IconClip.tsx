import { createIcon } from '../_createIcon/createIcon';

import IconClipSizeAll from './IconClip_size_all';

export const IconClip = createIcon({
  l: IconClipSizeAll,
  m: IconClipSizeAll,
  s: IconClipSizeAll,
  xs: IconClipSizeAll,
  name: 'IconClip',
});
