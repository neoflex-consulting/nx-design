import { createIcon } from '../_createIcon/createIcon';

import IconCheckDoubleSizeAll from './IconCheckDouble_size_all';

export const IconCheckDouble = createIcon({
  l: IconCheckDoubleSizeAll,
  m: IconCheckDoubleSizeAll,
  s: IconCheckDoubleSizeAll,
  xs: IconCheckDoubleSizeAll,
  name: 'IconCheckDouble',
});
