import { createIcon } from '../_createIcon/createIcon';

import IconTagsSizeAll from './IconTags_size_all';

export const IconTags = createIcon({
  l: IconTagsSizeAll,
  m: IconTagsSizeAll,
  s: IconTagsSizeAll,
  xs: IconTagsSizeAll,
  name: 'IconTags',
});
