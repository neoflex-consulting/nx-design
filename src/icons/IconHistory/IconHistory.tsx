import { createIcon } from '../_createIcon/createIcon';

import IconHistorySizeAll from './IconHistory_size_all';

export const IconHistory = createIcon({
  l: IconHistorySizeAll,
  m: IconHistorySizeAll,
  s: IconHistorySizeAll,
  xs: IconHistorySizeAll,
  name: 'IconHistory',
});
