import { createIcon } from '../_createIcon/createIcon';

import IconArrowUpSizeAll from './IconArrowUp_size_all';

export const IconArrowUp = createIcon({
  l: IconArrowUpSizeAll,
  m: IconArrowUpSizeAll,
  s: IconArrowUpSizeAll,
  xs: IconArrowUpSizeAll,
  name: 'IconArrowUp',
});
