import { createIcon } from '../_createIcon/createIcon';

import UploadSizeAll from './Upload_size_all';

export const Upload = createIcon({
  l: UploadSizeAll,
  m: UploadSizeAll,
  s: UploadSizeAll,
  xs: UploadSizeAll,
  name: 'IconUpload',
});
