import { createIcon } from '../_createIcon/createIcon';

import MinusSizeAll from './Minus_size_all';

export const Minus = createIcon({
  m: MinusSizeAll,
  s: MinusSizeAll,
  xs: MinusSizeAll,
  name: 'Minus',
});
