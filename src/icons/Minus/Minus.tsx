import { createIcon } from '../_createIcon/createIcon';

import IconMinusSizeAll from './IconMinus_size_all';

export const IconMinus = createIcon({
  l: IconMinusSizeAll,
  m: IconMinusSizeAll,
  s: IconMinusSizeAll,
  xs: IconMinusSizeAll,
  name: 'IconMinus',
});
