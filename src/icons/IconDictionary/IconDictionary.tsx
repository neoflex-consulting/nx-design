import { createIcon } from '../_createIcon/createIcon';

import IconDictionarySizeAll from './IconDictionary_size_all';

export const IconDictionary = createIcon({
  l: IconDictionarySizeAll,
  m: IconDictionarySizeAll,
  s: IconDictionarySizeAll,
  xs: IconDictionarySizeAll,
  name: 'IconDictionary',
});
