import { createIcon } from '../_createIcon/createIcon';

import CheckOnSizeAll from './CheckOn_size_all';

export const CheckOn = createIcon({
  l: CheckOnSizeAll,
  m: CheckOnSizeAll,
  s: CheckOnSizeAll,
  xs: CheckOnSizeAll,
  name: 'IconCheckOn',
});
