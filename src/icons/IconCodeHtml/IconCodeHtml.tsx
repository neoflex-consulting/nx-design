import { createIcon } from '../_createIcon/createIcon';

import IconCodeHtmlSizeAll from './IconCodeHtml_size_all';

export const IconCodeHtml = createIcon({
  l: IconCodeHtmlSizeAll,
  m: IconCodeHtmlSizeAll,
  s: IconCodeHtmlSizeAll,
  xs: IconCodeHtmlSizeAll,
  name: 'IconCodeHtml',
});
