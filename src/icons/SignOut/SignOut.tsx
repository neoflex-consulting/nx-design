import { createIcon } from '../_createIcon/createIcon';

import SignOutSizeAll from './SignOut_size_all';

export const SignOut = createIcon({
  m: SignOutSizeAll,
  s: SignOutSizeAll,
  xs: SignOutSizeAll,
  name: 'SignOut',
});
