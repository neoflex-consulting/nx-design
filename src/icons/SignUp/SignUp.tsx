import { createIcon } from '../_createIcon/createIcon';

import SignUpSizeAll from './SignUp_size_all';

export const SignUp = createIcon({
  m: SignUpSizeAll,
  s: SignUpSizeAll,
  xs: SignUpSizeAll,
  name: 'SignUp',
});
