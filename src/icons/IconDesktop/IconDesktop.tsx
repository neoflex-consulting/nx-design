import { createIcon } from '../_createIcon/createIcon';

import IconDesktopSizeAll from './IconDesktop_size_all';

export const IconDesktop = createIcon({
  l: IconDesktopSizeAll,
  m: IconDesktopSizeAll,
  s: IconDesktopSizeAll,
  xs: IconDesktopSizeAll,
  name: 'IconDesktop',
});
