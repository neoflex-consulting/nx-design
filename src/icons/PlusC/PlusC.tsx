import { createIcon } from '../_createIcon/createIcon';

import PlusCSizeAll from './PlusC_size_all';

export const PlusC = createIcon({
  m: PlusCSizeAll,
  s: PlusCSizeAll,
  xs: PlusCSizeAll,
  name: 'PlusC',
});
