import { createIcon } from '../_createIcon/createIcon';

import IconTagSizeAll from './IconTag_size_all';

export const IconTag = createIcon({
  l: IconTagSizeAll,
  m: IconTagSizeAll,
  s: IconTagSizeAll,
  xs: IconTagSizeAll,
  name: 'IconTag',
});
