import { createIcon } from '../_createIcon/createIcon';

import IconTextSizeAll from './IconText_size_all';

export const IconText = createIcon({
  l: IconTextSizeAll,
  m: IconTextSizeAll,
  s: IconTextSizeAll,
  xs: IconTextSizeAll,
  name: 'IconText',
});
