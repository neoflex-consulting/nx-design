import { createIcon } from '../_createIcon/createIcon';

import AsteriksSizeAll from './Asteriks_size_all';

export const Asteriks = createIcon({
  l: AsteriksSizeAll,
  m: AsteriksSizeAll,
  s: AsteriksSizeAll,
  xs: AsteriksSizeAll,
  name: 'IconAsteriks',
});
