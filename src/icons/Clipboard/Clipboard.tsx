import { createIcon } from '../_createIcon/createIcon';

import ClipboardSizeAll from './Clipboard_size_all';

export const Clipboard = createIcon({
  l: ClipboardSizeAll,
  m: ClipboardSizeAll,
  s: ClipboardSizeAll,
  xs: ClipboardSizeAll,
  name: 'Clipboard',
});
