import { createIcon } from '../_createIcon/createIcon';

import IconGridIconSizeAll from './IconGridIcon_size_all';

export const IconGrid = createIcon({
  l: IconGridIconSizeAll,
  m: IconGridIconSizeAll,
  s: IconGridIconSizeAll,
  xs: IconGridIconSizeAll,
  name: 'IconGrid',
});
