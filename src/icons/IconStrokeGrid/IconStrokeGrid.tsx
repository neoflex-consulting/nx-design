import { createIcon } from '../_createIcon/createIcon';

import IconStrokeGridSizeAll from './IconStrokeGrid_size_all';

export const IconStrokeGrid = createIcon({
  l: IconStrokeGridSizeAll,
  m: IconStrokeGridSizeAll,
  s: IconStrokeGridSizeAll,
  xs: IconStrokeGridSizeAll,
  name: 'IconStrokeGrid',
});
