import { createIcon } from '../_createIcon/createIcon';

import IconTrySizeAll from './IconTry_size_all';

export const IconTry = createIcon({
  l: IconTrySizeAll,
  m: IconTrySizeAll,
  s: IconTrySizeAll,
  xs: IconTrySizeAll,
  name: 'IconTry',
});
