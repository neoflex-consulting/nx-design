import { createIcon } from '../_createIcon/createIcon';

import ImportantSizeAll from './Important_size_all';

export const Important = createIcon({
  m: ImportantSizeAll,
  s: ImportantSizeAll,
  xs: ImportantSizeAll,
  name: 'Important',
});
