import { createIcon } from '../_createIcon/createIcon';

import InboxSizeAll from './Inbox_size_all';

export const Inbox = createIcon({
  l: InboxSizeAll,
  m: InboxSizeAll,
  s: InboxSizeAll,
  xs: InboxSizeAll,
  name: 'Inbox',
});
