import { createIcon } from '../_createIcon/createIcon';

import GlobeSizeAll from './Globe_size_all';

export const Globe = createIcon({
  l: GlobeSizeAll,
  m: GlobeSizeAll,
  s: GlobeSizeAll,
  xs: GlobeSizeAll,
  name: 'IconGlobe',
});
