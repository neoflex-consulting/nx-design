import { createIcon } from '../_createIcon/createIcon';

import IconToolboxSizeAll from './IconToolbox_size_all';

export const IconToolbox = createIcon({
  l: IconToolboxSizeAll,
  m: IconToolboxSizeAll,
  s: IconToolboxSizeAll,
  xs: IconToolboxSizeAll,
  name: 'IconToolbox',
});
