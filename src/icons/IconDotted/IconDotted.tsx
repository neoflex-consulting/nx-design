import { createIcon } from '../_createIcon/createIcon';

import IconDottedSizeAll from './IconDotted_size_all';

export const IconDotted = createIcon({
  l: IconDottedSizeAll,
  m: IconDottedSizeAll,
  s: IconDottedSizeAll,
  xs: IconDottedSizeAll,
  name: 'IconDotted',
});
