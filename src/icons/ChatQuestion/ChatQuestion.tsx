import { createIcon } from '../_createIcon/createIcon';

import ChatQuestionSizeAll from './ChatQuestion_size_all';

export const ChatQuestion = createIcon({
  m: ChatQuestionSizeAll,
  s: ChatQuestionSizeAll,
  xs: ChatQuestionSizeAll,
  name: 'ChatQuestion',
});
