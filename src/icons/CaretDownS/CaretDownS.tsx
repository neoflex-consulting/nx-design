import { createIcon } from '../_createIcon/createIcon';

import CaretDownSSizeAll from './CaretDownS_size_all';

export const CaretDownS = createIcon({
  l: CaretDownSSizeAll,
  m: CaretDownSSizeAll,
  s: CaretDownSSizeAll,
  xs: CaretDownSSizeAll,
  name: 'CaretDownS',
});
