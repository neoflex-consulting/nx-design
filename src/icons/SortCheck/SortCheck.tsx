import { createIcon } from '../_createIcon/createIcon';

import SortCheckSizeAll from './SortCheck_size_all';

export const SortCheck = createIcon({
  m: SortCheckSizeAll,
  s: SortCheckSizeAll,
  xs: SortCheckSizeAll,
  name: 'SortCheck',
});
