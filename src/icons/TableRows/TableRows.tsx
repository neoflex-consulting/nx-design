import { createIcon } from '../_createIcon/createIcon';

import TableRowsSizeAll from './TableRows_size_all';

export const TableRows = createIcon({
  l: TableRowsSizeAll,
  m: TableRowsSizeAll,
  s: TableRowsSizeAll,
  xs: TableRowsSizeAll,
  name: 'IconTableRows',
});
