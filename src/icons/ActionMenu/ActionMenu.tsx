import { createIcon } from '../_createIcon/createIcon';

import ActionMenuSizeAll from './ActionMenu_size_all';

export const ActionMenu = createIcon({
  l: ActionMenuSizeAll,
  m: ActionMenuSizeAll,
  s: ActionMenuSizeAll,
  xs: ActionMenuSizeAll,
  name: 'ActionMenu'
});
