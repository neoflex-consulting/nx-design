import { createIcon } from '../_createIcon/createIcon';

import IconDragSizeAll from './IconDrag_size_all';

export const IconDrag = createIcon({
  l: IconDragSizeAll,
  m: IconDragSizeAll,
  s: IconDragSizeAll,
  xs: IconDragSizeAll,
  name: 'IconDrag',
});
