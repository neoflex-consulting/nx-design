import { createIcon } from '../_createIcon/createIcon';

import HeartSizeAll from './Heart_size_all';

export const Heart = createIcon({
  l: HeartSizeAll,
  m: HeartSizeAll,
  s: HeartSizeAll,
  xs: HeartSizeAll,
  name: 'IconHeart',
});
