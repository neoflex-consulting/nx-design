import { createIcon } from '../_createIcon/createIcon';

import IconFullScreenSizeAll from './IconFullScreen_size_all';

export const IconFullScreen = createIcon({
  l: IconFullScreenSizeAll,
  m: IconFullScreenSizeAll,
  s: IconFullScreenSizeAll,
  xs: IconFullScreenSizeAll,
  name: 'IconFullScreen',
});
