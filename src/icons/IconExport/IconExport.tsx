import { createIcon } from '../_createIcon/createIcon';

import IconExportSizeAll from './IconExport_size_all';

export const IconExport = createIcon({
  l: IconExportSizeAll,
  m: IconExportSizeAll,
  s: IconExportSizeAll,
  xs: IconExportSizeAll,
  name: 'IconExport',
});
