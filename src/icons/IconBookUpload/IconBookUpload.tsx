import { createIcon } from '../_createIcon/createIcon';

import IconBookUploadSizeAll from './IconBookUpload_size_all';

export const IconBookUpload = createIcon({
  l: IconBookUploadSizeAll,
  m: IconBookUploadSizeAll,
  s: IconBookUploadSizeAll,
  xs: IconBookUploadSizeAll,
  name: 'IconBookUpload',
});
