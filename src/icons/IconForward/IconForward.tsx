import { createIcon } from '../_createIcon/createIcon';

import IconForwardSizeAll from './IconForward_size_all';

export const IconForward = createIcon({
  l: IconForwardSizeAll,
  m: IconForwardSizeAll,
  s: IconForwardSizeAll,
  xs: IconForwardSizeAll,
  name: 'IconForward',
});
