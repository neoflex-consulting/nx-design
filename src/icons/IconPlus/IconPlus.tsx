import { createIcon } from '../_createIcon/createIcon';

import IconPlusSizeAll from './IconPlus_size_all';

export const IconPlus = createIcon({
  l: IconPlusSizeAll,
  m: IconPlusSizeAll,
  s: IconPlusSizeAll,
  xs: IconPlusSizeAll,
  name: 'IconPlus',
});
