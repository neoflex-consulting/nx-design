import { createIcon } from '../_createIcon/createIcon';

import IconControlSizeAll from './IconControl_size_all';

export const IconControl = createIcon({
  l: IconControlSizeAll,
  m: IconControlSizeAll,
  s: IconControlSizeAll,
  xs: IconControlSizeAll,
  name: 'IconControl',
});
