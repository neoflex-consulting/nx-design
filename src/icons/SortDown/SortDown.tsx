import { createIcon } from '../_createIcon/createIcon';

import SortDownSizeAll from './SortDown_size_all';

export const SortDown = createIcon({
  l: SortDownSizeAll,
  m: SortDownSizeAll,
  s: SortDownSizeAll,
  xs: SortDownSizeAll,
  name: 'IconSortDown',
});
