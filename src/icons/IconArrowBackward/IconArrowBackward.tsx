import { createIcon } from '../_createIcon/createIcon';

import IconArrowBackwardSizeAll from './IconArrowBackward_size_all';

export const IconArrowBackward = createIcon({
  l: IconArrowBackwardSizeAll,
  m: IconArrowBackwardSizeAll,
  s: IconArrowBackwardSizeAll,
  xs: IconArrowBackwardSizeAll,
  name: 'IconArrowBackward'
});
