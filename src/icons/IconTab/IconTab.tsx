import { createIcon } from '../_createIcon/createIcon';

import IconTabSizeAll from './IconTab_size_all';

export const IconTab = createIcon({
  l: IconTabSizeAll,
  m: IconTabSizeAll,
  s: IconTabSizeAll,
  xs: IconTabSizeAll,
  name: 'IconTab',
});
