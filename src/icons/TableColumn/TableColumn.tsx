import { createIcon } from '../_createIcon/createIcon';

import TableColumnSizeAll from './TableColumn_size_all';

export const TableColumn = createIcon({
  m: TableColumnSizeAll,
  s: TableColumnSizeAll,
  xs: TableColumnSizeAll,
  name: 'TableColumn',
});
