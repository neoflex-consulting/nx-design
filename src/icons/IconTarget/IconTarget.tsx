import { createIcon } from '../_createIcon/createIcon';

import IconTargetSizeAll from './IconTarget_size_all';

export const IconTarget = createIcon({
  l: IconTargetSizeAll,
  m: IconTargetSizeAll,
  s: IconTargetSizeAll,
  xs: IconTargetSizeAll,
  name: 'IconTarget',
});
