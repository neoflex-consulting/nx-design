import { createIcon } from '../_createIcon/createIcon';

import IconTextAreaSizeAll from './IconTextArea_size_all';

export const IconTextArea = createIcon({
  l: IconTextAreaSizeAll,
  m: IconTextAreaSizeAll,
  s: IconTextAreaSizeAll,
  xs: IconTextAreaSizeAll,
  name: 'IconTextArea',
});
