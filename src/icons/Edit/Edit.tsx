import { createIcon } from '../_createIcon/createIcon';

import EditSizeAll from './Edit_size_all';

export const Edit = createIcon({
  m: EditSizeAll,
  s: EditSizeAll,
  xs: EditSizeAll,
  name: 'Edit',
});
