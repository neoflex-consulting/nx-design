import { createIcon } from '../_createIcon/createIcon';

import WarningCSizeAll from './WarningC_size_all';

export const WarningC = createIcon({
  l: WarningCSizeAll,
  m: WarningCSizeAll,
  s: WarningCSizeAll,
  xs: WarningCSizeAll,
  name: 'IconWarningC',
});
