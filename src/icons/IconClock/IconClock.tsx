import { createIcon } from '../_createIcon/createIcon';

import IconClockSizeAll from './IconClock_size_all';

export const IconClock = createIcon({
  l: IconClockSizeAll,
  m: IconClockSizeAll,
  s: IconClockSizeAll,
  xs: IconClockSizeAll,
  name: 'IconClock',
});
