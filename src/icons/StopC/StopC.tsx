import { createIcon } from '../_createIcon/createIcon';

import StopCSizeAll from './StopC_size_all';

export const StopC = createIcon({
  m: StopCSizeAll,
  s: StopCSizeAll,
  xs: StopCSizeAll,
  name: 'StopC',
});
