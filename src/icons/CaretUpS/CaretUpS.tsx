import { createIcon } from '../_createIcon/createIcon';

import CaretUpSSizeAll from './CaretUpS_size_all';

export const CaretUpS = createIcon({
  m: CaretUpSSizeAll,
  s: CaretUpSSizeAll,
  xs: CaretUpSSizeAll,
  name: 'CaretUpS',
});
