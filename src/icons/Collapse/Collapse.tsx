import { createIcon } from '../_createIcon/createIcon';

import CollapseSizeAll from './Collapse_size_all';

export const Collapse = createIcon({
  m: CollapseSizeAll,
  s: CollapseSizeAll,
  xs: CollapseSizeAll,
  name: 'Collapse',
});
