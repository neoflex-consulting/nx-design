import { createIcon } from '../_createIcon/createIcon';

import CloudSizeAll from './Cloud_size_all';

export const Cloud = createIcon({
  l: CloudSizeAll,
  m: CloudSizeAll,
  s: CloudSizeAll,
  xs: CloudSizeAll,
  name: 'IconCloud',
});
