import { createIcon } from '../_createIcon/createIcon';

import CalendarMinusSizeAll from './CalendarMinus_size_all';

export const CalendarMinus = createIcon({
  l: CalendarMinusSizeAll,
  m: CalendarMinusSizeAll,
  s: CalendarMinusSizeAll,
  xs: CalendarMinusSizeAll,
  name: 'CalendarMinus',
});
