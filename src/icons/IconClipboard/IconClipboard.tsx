import { createIcon } from '../_createIcon/createIcon';

import IconClipboardSizeAll from './IconClipboard_size_all';

export const IconClipboard = createIcon({
  l: IconClipboardSizeAll,
  m: IconClipboardSizeAll,
  s: IconClipboardSizeAll,
  xs: IconClipboardSizeAll,
  name: 'IconClipboard',
});
