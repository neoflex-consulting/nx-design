import { createIcon } from '../_createIcon/createIcon';

import ArrowUpSizeAll from './ArrowUp_size_all';

export const ArrowUp = createIcon({
  l: ArrowUpSizeAll,
  m: ArrowUpSizeAll,
  s: ArrowUpSizeAll,
  xs: ArrowUpSizeAll,
  name: 'IconArrowUp',
});
