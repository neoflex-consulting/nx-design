import { createIcon } from '../_createIcon/createIcon';

import FilePlusSizeAll from './FilePlus_size_all';

export const FilePlus = createIcon({
  l: FilePlusSizeAll,
  m: FilePlusSizeAll,
  s: FilePlusSizeAll,
  xs: FilePlusSizeAll,
  name: 'IconFilePlus',
});
