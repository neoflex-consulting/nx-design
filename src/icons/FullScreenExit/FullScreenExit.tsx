import { createIcon } from '../_createIcon/createIcon';

import FullScreenExitSizeAll from './FullScreenExit_size_all';

export const FullScreenExit = createIcon({
  l: FullScreenExitSizeAll,
  m: FullScreenExitSizeAll,
  s: FullScreenExitSizeAll,
  xs: FullScreenExitSizeAll,
  name: 'IconFullScreenExit',
});
