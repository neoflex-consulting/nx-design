import { createIcon } from '../_createIcon/createIcon';

import LocationMapSizeAll from './LocationMap_size_all';

export const LocationMap = createIcon({
  l: LocationMapSizeAll,
  m: LocationMapSizeAll,
  s: LocationMapSizeAll,
  xs: LocationMapSizeAll,
  name: 'LocationMap',
});
