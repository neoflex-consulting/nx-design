import { createIcon } from '../_createIcon/createIcon';

import IconCheckStatusSizeAll from './IconCheckStatus_size_all';

export const IconCheckStatus = createIcon({
  l: IconCheckStatusSizeAll,
  m: IconCheckStatusSizeAll,
  s: IconCheckStatusSizeAll,
  xs: IconCheckStatusSizeAll,
  name: 'IconCheckStatus',
});
