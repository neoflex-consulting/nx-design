import { createIcon } from '../_createIcon/createIcon';

import LockOnSizeAll from './LockOn_size_all';

export const LockOn = createIcon({
  m: LockOnSizeAll,
  s: LockOnSizeAll,
  xs: LockOnSizeAll,
  name: 'LockOn',
});
