import { createIcon } from '../_createIcon/createIcon';

import HomeSizeAll from './Home_size_all';

export const Home = createIcon({
  m: HomeSizeAll,
  s: HomeSizeAll,
  xs: HomeSizeAll,
  name: 'Home',
});
