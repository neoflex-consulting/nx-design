import { createIcon } from '../_createIcon/createIcon';

import BookSizeAll from './Book_size_all';

export const Book = createIcon({
  l: BookSizeAll,
  m: BookSizeAll,
  s: BookSizeAll,
  xs: BookSizeAll,
  name: 'IconBook',
});
