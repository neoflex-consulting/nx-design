import { createIcon } from '../_createIcon/createIcon';

import BankSizeAll from './Bank_size_all';

export const Bank = createIcon({
  l: BankSizeAll,
  m: BankSizeAll,
  s: BankSizeAll,
  xs: BankSizeAll,
  name: 'IconBank',
});
