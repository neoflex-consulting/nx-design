import { createIcon } from '../_createIcon/createIcon';

import FilterSizeAll from './Filter_size_all';

export const Filter = createIcon({
  l: FilterSizeAll,
  m: FilterSizeAll,
  s: FilterSizeAll,
  xs: FilterSizeAll,
  name: 'Filter',
});
