import { createIcon } from '../_createIcon/createIcon';

import IconToolsSizeAll from './IconTools_size_all';

export const IconTools = createIcon({
  l: IconToolsSizeAll,
  m: IconToolsSizeAll,
  s: IconToolsSizeAll,
  xs: IconToolsSizeAll,
  name: 'IconTools',
});
