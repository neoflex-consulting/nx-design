import { createIcon } from '../_createIcon/createIcon';

import SyncSizeAll from './Sync_size_all';

export const Sync = createIcon({
  l: SyncSizeAll,
  m: SyncSizeAll,
  s: SyncSizeAll,
  xs: SyncSizeAll,
  name: 'Sync',
});
