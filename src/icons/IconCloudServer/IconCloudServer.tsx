import { createIcon } from '../_createIcon/createIcon';

import IconCloudServerSizeAll from './IconCloudServer_size_all';

export const IconCloudServer = createIcon({
  l: IconCloudServerSizeAll,
  m: IconCloudServerSizeAll,
  s: IconCloudServerSizeAll,
  xs: IconCloudServerSizeAll,
  name: 'IconCloudServer',
});
