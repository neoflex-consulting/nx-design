import { createIcon } from '../_createIcon/createIcon';

import IconInfoSizeAll from './IconInfo_size_all';

export const IconInfo = createIcon({
  l: IconInfoSizeAll,
  m: IconInfoSizeAll,
  s: IconInfoSizeAll,
  xs: IconInfoSizeAll,
  name: 'IconInfo',
});
