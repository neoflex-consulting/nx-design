import { createIcon } from '../_createIcon/createIcon';

import IconFilePlusSizeAll from './IconFilePlus_size_all';

export const IconFilePlus = createIcon({
  l: IconFilePlusSizeAll,
  m: IconFilePlusSizeAll,
  s: IconFilePlusSizeAll,
  xs: IconFilePlusSizeAll,
  name: 'IconFilePlus',
});
