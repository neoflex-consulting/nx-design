import { createIcon } from '../_createIcon/createIcon';

import IconStepSizeAll from './IconStep_size_all';

export const IconStep = createIcon({
  l: IconStepSizeAll,
  m: IconStepSizeAll,
  s: IconStepSizeAll,
  xs: IconStepSizeAll,
  name: 'IconStep',
});
