import { createIcon } from '../_createIcon/createIcon';

import IconCheckFullSizeAll from './IconCheckFull_size_all';

export const IconCheckFull = createIcon({
  l: IconCheckFullSizeAll,
  m: IconCheckFullSizeAll,
  s: IconCheckFullSizeAll,
  xs: IconCheckFullSizeAll,
  name: 'IconCheckFull',
});
