import { createIcon } from '../_createIcon/createIcon';

import IconStepsCSizeAll from './IconStepsC_size_all';

export const IconStepsC = createIcon({
  l: IconStepsCSizeAll,
  m: IconStepsCSizeAll,
  s: IconStepsCSizeAll,
  xs: IconStepsCSizeAll,
  name: 'IconStepsC',
});
