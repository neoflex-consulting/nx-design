import { createIcon } from '../_createIcon/createIcon';

import StarSizeAll from './Star_size_all';

export const Star = createIcon({
  l: StarSizeAll,
  m: StarSizeAll,
  s: StarSizeAll,
  xs: StarSizeAll,
  name: 'Star',
});
