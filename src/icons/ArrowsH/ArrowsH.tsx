import { createIcon } from '../createIcon/createIcon';

import ArrowsHSizeAll from './ArrowsH_size_all';

export const ArrowsH = createIcon({
  m: ArrowsHSizeAll,
  s: ArrowsHSizeAll,
  xs: ArrowsHSizeAll,
  name: 'ArrowsH',
});
