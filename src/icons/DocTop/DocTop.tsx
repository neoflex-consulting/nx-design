import { createIcon } from '../_createIcon/createIcon';

import DocTopSizeAll from './DocTop_size_all';

export const DocTop = createIcon({
  l: DocTopSizeAll,
  m: DocTopSizeAll,
  s: DocTopSizeAll,
  xs: DocTopSizeAll,
  name: 'DocTop',
});
