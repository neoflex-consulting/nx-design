import { createIcon } from '../_createIcon/createIcon';

import CheckDoubleSizeAll from './CheckDouble_size_all';

export const CheckDouble = createIcon({
  l: CheckDoubleSizeAll,
  m: CheckDoubleSizeAll,
  s: CheckDoubleSizeAll,
  xs: CheckDoubleSizeAll,
  name: 'CheckDouble',
});
