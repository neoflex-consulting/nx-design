import { createIcon } from '../_createIcon/createIcon';

import SystemSettingsSizeAll from './SystemSettings_size_all';

export const SystemSettings = createIcon({
  l: SystemSettingsSizeAll,
  m: SystemSettingsSizeAll,
  s: SystemSettingsSizeAll,
  xs: SystemSettingsSizeAll,
  name: 'SystemSettings',
});
