import { createIcon } from '../_createIcon/createIcon';

import SliderSizeAll from './Slider_size_all';

export const Slider = createIcon({
  l: SliderSizeAll,
  m: SliderSizeAll,
  s: SliderSizeAll,
  xs: SliderSizeAll,
  name: 'IconSlider',
});
