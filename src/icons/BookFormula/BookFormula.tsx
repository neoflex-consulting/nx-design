import { createIcon } from '../_createIcon/createIcon';

import BookFormulaSizeAll from './BookFormula_size_all';

export const BookFormula = createIcon({
  l: BookFormulaSizeAll,
  m: BookFormulaSizeAll,
  s: BookFormulaSizeAll,
  xs: BookFormulaSizeAll,
  name: 'BookFormula',
});
