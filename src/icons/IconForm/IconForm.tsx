import { createIcon } from '../_createIcon/createIcon';

import IconFormSizeAll from './IconForm_size_all';

export const IconForm = createIcon({
  l: IconFormSizeAll,
  m: IconFormSizeAll,
  s: IconFormSizeAll,
  xs: IconFormSizeAll,
  name: 'IconForm',
});
