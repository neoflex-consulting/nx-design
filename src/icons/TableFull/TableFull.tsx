import { createIcon } from '../_createIcon/createIcon';

import TableFullSizeAll from './TableFull_size_all';

export const TableFull = createIcon({
  l: TableFullSizeAll,
  m: TableFullSizeAll,
  s: TableFullSizeAll,
  xs: TableFullSizeAll,
  name: 'IconTableFull',
});
