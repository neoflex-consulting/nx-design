import { createIcon } from '../_createIcon/createIcon';

import TagsSizeAll from './Tags_size_all';

export const Tags = createIcon({
  l: TagsSizeAll,
  m: TagsSizeAll,
  s: TagsSizeAll,
  xs: TagsSizeAll,
  name: 'Tags',
});
