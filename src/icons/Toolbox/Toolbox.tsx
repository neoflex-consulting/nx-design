import { createIcon } from '../_createIcon/createIcon';

import ToolboxSizeAll from './Toolbox_size_all';

export const Toolbox = createIcon({
  l: ToolboxSizeAll,
  m: ToolboxSizeAll,
  s: ToolboxSizeAll,
  xs: ToolboxSizeAll,
  name: 'Toolbox',
});
