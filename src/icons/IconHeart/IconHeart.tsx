import { createIcon } from '../_createIcon/createIcon';

import IconHeartSizeAll from './IconHeart_size_all';

export const IconHeart = createIcon({
  l: IconHeartSizeAll,
  m: IconHeartSizeAll,
  s: IconHeartSizeAll,
  xs: IconHeartSizeAll,
  name: 'IconHeart',
});
