import { createIcon } from '../_createIcon/createIcon';

import TagSizeAll from './Tag_size_all';

export const Tag = createIcon({
  m: TagSizeAll,
  s: TagSizeAll,
  xs: TagSizeAll,
  name: 'Tag',
});
