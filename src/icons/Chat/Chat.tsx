import { createIcon } from '../_createIcon/createIcon';

import ChatSizeAll from './Chat_size_all';

export const Chat = createIcon({
  l: ChatSizeAll,
  m: ChatSizeAll,
  s: ChatSizeAll,
  xs: ChatSizeAll,
  name: 'IconChat',
});
