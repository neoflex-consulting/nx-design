import { createIcon } from '../_createIcon/createIcon';

import DownSizeAll from './Down_size_all';

export const Down = createIcon({
  l: DownSizeAll,
  m: DownSizeAll,
  s: DownSizeAll,
  xs: DownSizeAll,
  name: 'Down',
});
