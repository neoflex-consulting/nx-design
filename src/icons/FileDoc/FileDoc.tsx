import { createIcon } from '../_createIcon/createIcon';

import FileDocSizeAll from './FileDoc_size_all';

export const FileDoc = createIcon({
  l: FileDocSizeAll,
  m: FileDocSizeAll,
  s: FileDocSizeAll,
  xs: FileDocSizeAll,
  name: 'FileDoc',
});
