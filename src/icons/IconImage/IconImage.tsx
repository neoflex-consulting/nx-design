import { createIcon } from '../_createIcon/createIcon';

import IconImageSizeAll from './IconImage_size_all';

export const IconImage = createIcon({
  l: IconImageSizeAll,
  m: IconImageSizeAll,
  s: IconImageSizeAll,
  xs: IconImageSizeAll,
  name: 'IconImage',
});
