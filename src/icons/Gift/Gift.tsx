import { createIcon } from '../_createIcon/createIcon';

import GiftSizeAll from './Gift_size_all';

export const Gift = createIcon({
  m: GiftSizeAll,
  s: GiftSizeAll,
  xs: GiftSizeAll,
  name: 'Gift',
});
