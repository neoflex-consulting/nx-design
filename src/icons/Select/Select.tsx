import { createIcon } from '../_createIcon/createIcon';

import SelectSizeAll from './Select_size_all';

export const Select = createIcon({
  m: SelectSizeAll,
  s: SelectSizeAll,
  xs: SelectSizeAll,
  name: 'Select',
});
