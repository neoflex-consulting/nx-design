import { createIcon } from '../_createIcon/createIcon';

import DocRightSizeAll from './DocRight_size_all';

export const DocRight = createIcon({
  l: DocRightSizeAll,
  m: DocRightSizeAll,
  s: DocRightSizeAll,
  xs: DocRightSizeAll,
  name: 'IconDocRight',
});
