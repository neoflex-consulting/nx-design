import { createIcon } from '../_createIcon/createIcon';

import FileAiSizeAll from './FileAi_size_all';

export const FileAi = createIcon({
  l: FileAiSizeAll,
  m: FileAiSizeAll,
  s: FileAiSizeAll,
  xs: FileAiSizeAll,
  name: 'FileAi',
});
