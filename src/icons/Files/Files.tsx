import { createIcon } from '../_createIcon/createIcon';

import FilesSizeAll from './Files_size_all';

export const Files = createIcon({
  l: FilesSizeAll,
  m: FilesSizeAll,
  s: FilesSizeAll,
  xs: FilesSizeAll,
  name: 'Files',
});
