import { createIcon } from '../_createIcon/createIcon';

import CalendarDateSizeAll from './CalendarDate_size_all';

export const CalendarDate = createIcon({
  l: CalendarDateSizeAll,
  m: CalendarDateSizeAll,
  s: CalendarDateSizeAll,
  xs: CalendarDateSizeAll,
  name: 'CalendarDate',
});
