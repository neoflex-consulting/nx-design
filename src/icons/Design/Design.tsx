import { createIcon } from '../_createIcon/createIcon';

import DesignSizeAll from './Design_size_all';

export const Design = createIcon({
  l: DesignSizeAll,
  m: DesignSizeAll,
  s: DesignSizeAll,
  xs: DesignSizeAll,
  name: 'IconDesign',
});
