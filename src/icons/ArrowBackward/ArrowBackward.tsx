import { createIcon } from '../_createIcon/createIcon';

import ArrowBackwardSizeAll from './ArrowBackward_size_all';

export const ArrowBackward = createIcon({
  l: ArrowBackwardSizeAll,
  m: ArrowBackwardSizeAll,
  s: ArrowBackwardSizeAll,
  xs: ArrowBackwardSizeAll,
  name: 'IconArrowBackward'
});
