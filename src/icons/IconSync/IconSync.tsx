import { createIcon } from '../_createIcon/createIcon';

import IconSyncSizeAll from './IconSync_size_all';

export const IconSync = createIcon({
  l: IconSyncSizeAll,
  m: IconSyncSizeAll,
  s: IconSyncSizeAll,
  xs: IconSyncSizeAll,
  name: 'IconSync',
});
