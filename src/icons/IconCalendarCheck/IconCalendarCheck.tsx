import { createIcon } from '../_createIcon/createIcon';

import IconCalendarCheckSizeAll from './IconCalendarCheck_size_all';

export const IconCalendarCheck = createIcon({
  l: IconCalendarCheckSizeAll,
  m: IconCalendarCheckSizeAll,
  s: IconCalendarCheckSizeAll,
  xs: IconCalendarCheckSizeAll,
  name: 'IconCalendarCheck',
});
