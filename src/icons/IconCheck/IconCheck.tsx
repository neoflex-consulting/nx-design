import { createIcon } from '../_createIcon/createIcon';

import IconCheckSizeAll from './IconCheck_size_all';

export const IconCheck = createIcon({
  l: IconCheckSizeAll,
  m: IconCheckSizeAll,
  s: IconCheckSizeAll,
  xs: IconCheckSizeAll,
  name: 'IconCheck',
});
