import { createIcon } from '../_createIcon/createIcon';

import IconRubSizeAll from './IconRub_size_all';

export const IconRub = createIcon({
  l: IconRubSizeAll,
  m: IconRubSizeAll,
  s: IconRubSizeAll,
  xs: IconRubSizeAll,
  name: 'IconRub',
});
