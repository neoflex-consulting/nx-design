import { createIcon } from '../_createIcon/createIcon';

import RubSizeAll from './Rub_size_all';

export const Rub = createIcon({
  l: RubSizeAll,
  m: RubSizeAll,
  s: RubSizeAll,
  xs: RubSizeAll,
  name: 'Rub',
});
