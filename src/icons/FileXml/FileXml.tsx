import { createIcon } from '../_createIcon/createIcon';

import FileXmlSizeAll from './FileXml_size_all';

export const FileXml = createIcon({
  m: FileXmlSizeAll,
  s: FileXmlSizeAll,
  xs: FileXmlSizeAll,
  name: 'FileXml',
});
