import { createIcon } from '../_createIcon/createIcon';

import MenuOpenSizeAll from './MenuOpen_size_all';

export const MenuOpen = createIcon({
  l: MenuOpenSizeAll,
  m: MenuOpenSizeAll,
  s: MenuOpenSizeAll,
  xs: MenuOpenSizeAll,
  name: 'IconMenuOpen',
});
