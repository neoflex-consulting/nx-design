import { createIcon } from '../_createIcon/createIcon';

import IconArrowDownSizeAll from './IconArrowDown_size_all';

export const IconArrowDown = createIcon({
  l: IconArrowDownSizeAll,
  m: IconArrowDownSizeAll,
  s: IconArrowDownSizeAll,
  xs: IconArrowDownSizeAll,
  name: 'IconArrowDown'
});
