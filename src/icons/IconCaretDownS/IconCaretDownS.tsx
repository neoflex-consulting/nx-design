import { createIcon } from '../_createIcon/createIcon';

import IconCaretDownSSizeAll from './IconCaretDownS_size_all';

export const IconCaretDownS = createIcon({
  l: IconCaretDownSSizeAll,
  m: IconCaretDownSSizeAll,
  s: IconCaretDownSSizeAll,
  xs: IconCaretDownSSizeAll,
  name: 'IconCaretDownS',
});
