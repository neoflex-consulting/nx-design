import { createIcon } from '../_createIcon/createIcon';

import GridSizeAll from './Grid_size_all';

export const Grid = createIcon({
  l: GridSizeAll,
  m: GridSizeAll,
  s: GridSizeAll,
  xs: GridSizeAll,
  name: 'Grid',
});
