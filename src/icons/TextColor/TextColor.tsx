import { createIcon } from '../_createIcon/createIcon';

import TextColorSizeAll from './TextColor_size_all';

export const TextColor = createIcon({
  m: TextColorSizeAll,
  s: TextColorSizeAll,
  xs: TextColorSizeAll,
  name: 'TextColor',
});
