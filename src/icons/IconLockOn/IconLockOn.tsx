import { createIcon } from '../_createIcon/createIcon';

import IconLockOnSizeAll from './IconLockOn_size_all';

export const IconLockOn = createIcon({
  l: IconLockOnSizeAll,
  m: IconLockOnSizeAll,
  s: IconLockOnSizeAll,
  xs: IconLockOnSizeAll,
  name: 'IconLockOn',
});
