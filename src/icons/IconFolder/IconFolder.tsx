import { createIcon } from '../_createIcon/createIcon';

import IconFolderSizeAll from './IconFolder_size_all';

export const IconFolder = createIcon({
  l: IconFolderSizeAll,
  m: IconFolderSizeAll,
  s: IconFolderSizeAll,
  xs: IconFolderSizeAll,
  name: 'IconFolder',
});
