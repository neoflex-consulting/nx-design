import { createIcon } from '../_createIcon/createIcon';

import UsdSizeAll from './Usd_size_all';

export const Usd = createIcon({
  l: UsdSizeAll,
  m: UsdSizeAll,
  s: UsdSizeAll,
  xs: UsdSizeAll,
  name: 'IconUsd',
});
