import { createIcon } from '../_createIcon/createIcon';

import IconFileSizeAll from './IconFile_size_all';

export const IconFile = createIcon({
  l: IconFileSizeAll,
  m: IconFileSizeAll,
  s: IconFileSizeAll,
  xs: IconFileSizeAll,
  name: 'IconFile',
});
