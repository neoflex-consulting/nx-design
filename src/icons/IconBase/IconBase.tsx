import { createIcon } from '../_createIcon/createIcon';

import IconBaseSizeAll from './IconBase_size_all';

export const IconBase = createIcon({
  l: IconBaseSizeAll,
  m: IconBaseSizeAll,
  s: IconBaseSizeAll,
  xs: IconBaseSizeAll,
  name: 'IconBase',
});
