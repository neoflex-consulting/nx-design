import { createIcon } from '../_createIcon/createIcon';

import StatusSizeAll from './Status_size_all';

export const Status = createIcon({
  l: StatusSizeAll,
  m: StatusSizeAll,
  s: StatusSizeAll,
  xs: StatusSizeAll,
  name: 'Status',
});
