import { createIcon } from '../_createIcon/createIcon';

import FileUploadSizeAll from './FileUpload_size_all';

export const FileUpload = createIcon({
  l: FileUploadSizeAll,
  m: FileUploadSizeAll,
  s: FileUploadSizeAll,
  xs: FileUploadSizeAll,
  name: 'IconFileUpload',
});
