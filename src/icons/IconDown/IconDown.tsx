import { createIcon } from '../_createIcon/createIcon';

import IconDownSizeAll from './IconDown_size_all';

export const IconDown = createIcon({
  l: IconDownSizeAll,
  m: IconDownSizeAll,
  s: IconDownSizeAll,
  xs: IconDownSizeAll,
  name: 'IconDown',
});
