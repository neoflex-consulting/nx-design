import { createIcon } from '../_createIcon/createIcon';

import IconUpSizeAll from './IconUp_size_all';

export const IconUp = createIcon({
  l: IconUpSizeAll,
  m: IconUpSizeAll,
  s: IconUpSizeAll,
  xs: IconUpSizeAll,
  name: 'IconUp',
});
