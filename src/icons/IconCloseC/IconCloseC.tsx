import { createIcon } from '../_createIcon/createIcon';

import IconCloseCSizeAll from './IconCloseC_size_all';

export const IconCloseC = createIcon({
  l: IconCloseCSizeAll,
  m: IconCloseCSizeAll,
  s: IconCloseCSizeAll,
  xs: IconCloseCSizeAll,
  name: 'IconCloseC',
});
