import { createIcon } from '../_createIcon/createIcon';

import IconSertificatSizeAll from './IconSertificat_size_all';

export const IconSertificat = createIcon({
  l: IconSertificatSizeAll,
  m: IconSertificatSizeAll,
  s: IconSertificatSizeAll,
  xs: IconSertificatSizeAll,
  name: 'IconSertificat',
});
