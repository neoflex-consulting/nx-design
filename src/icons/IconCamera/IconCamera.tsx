import { createIcon } from '../_createIcon/createIcon';

import IconCameraSizeAll from './IconCamera_size_all';

export const IconCamera = createIcon({
  l: IconCameraSizeAll,
  m: IconCameraSizeAll,
  s: IconCameraSizeAll,
  xs: IconCameraSizeAll,
  name: 'IconCamera',
});
