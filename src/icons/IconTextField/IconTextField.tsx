import { createIcon } from '../_createIcon/createIcon';

import IconTextFieldSizeAll from './IconTextField_size_all';

export const IconTextField = createIcon({
  l: IconTextFieldSizeAll,
  m: IconTextFieldSizeAll,
  s: IconTextFieldSizeAll,
  xs: IconTextFieldSizeAll,
  name: 'IconTextField',
});
