import { createIcon } from '../_createIcon/createIcon';

import SpinnerSizeAll from './Spinner_size_all';

export const Spinner = createIcon({
  l: SpinnerSizeAll,
  m: SpinnerSizeAll,
  s: SpinnerSizeAll,
  xs: SpinnerSizeAll,
  name: 'Spinner',
});
