import { createIcon } from '../_createIcon/createIcon';

import InfoSizeAll from './Info_size_all';

export const Info = createIcon({
  l: InfoSizeAll,
  m: InfoSizeAll,
  s: InfoSizeAll,
  xs: InfoSizeAll,
  name: 'IconInfo',
});
