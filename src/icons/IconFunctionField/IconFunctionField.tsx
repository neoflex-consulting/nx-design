import { createIcon } from '../_createIcon/createIcon';

import IconFunctionFieldSizeAll from './IconFunctionField_size_all';

export const IconFunctionField = createIcon({
  l: IconFunctionFieldSizeAll,
  m: IconFunctionFieldSizeAll,
  s: IconFunctionFieldSizeAll,
  xs: IconFunctionFieldSizeAll,
  name: 'IconFunctionField',
});
