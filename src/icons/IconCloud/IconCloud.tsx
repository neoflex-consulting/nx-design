import { createIcon } from '../_createIcon/createIcon';

import IconCloudSizeAll from './IconCloud_size_all';

export const IconCloud = createIcon({
  l: IconCloudSizeAll,
  m: IconCloudSizeAll,
  s: IconCloudSizeAll,
  xs: IconCloudSizeAll,
  name: 'IconCloud',
});
