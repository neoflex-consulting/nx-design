import { createIcon } from '../_createIcon/createIcon';

import StrokeGridSizeAll from './StrokeGrid_size_all';

export const StrokeGrid = createIcon({
  l: StrokeGridSizeAll,
  m: StrokeGridSizeAll,
  s: StrokeGridSizeAll,
  xs: StrokeGridSizeAll,
  name: 'StrokeGrid',
});
