import { createIcon } from '../_createIcon/createIcon';

import CheckCSizeAll from './CheckC_size_all';

export const CheckC = createIcon({
  l: CheckCSizeAll,
  m: CheckCSizeAll,
  s: CheckCSizeAll,
  xs: CheckCSizeAll,
  name: 'CheckC',
});
