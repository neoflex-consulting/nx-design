import { createIcon } from '../_createIcon/createIcon';

import IconWarningCSizeAll from './IconWarningC_size_all';

export const IconWarningC = createIcon({
  l: IconWarningCSizeAll,
  m: IconWarningCSizeAll,
  s: IconWarningCSizeAll,
  xs: IconWarningCSizeAll,
  name: 'IconWarningC',
});
