import { createIcon } from '../_createIcon/createIcon';

import IconCircleSizeAll from './IconCircle_size_all';

export const IconCircle = createIcon({
  l: IconCircleSizeAll,
  m: IconCircleSizeAll,
  s: IconCircleSizeAll,
  xs: IconCircleSizeAll,
  name: 'IconCircle',
});
