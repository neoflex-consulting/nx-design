import { createIcon } from '../_createIcon/createIcon';

import StepsCSizeAll from './StepsC_size_all';

export const StepsC = createIcon({
  m: StepsCSizeAll,
  s: StepsCSizeAll,
  xs: StepsCSizeAll,
  name: 'StepsC',
});
