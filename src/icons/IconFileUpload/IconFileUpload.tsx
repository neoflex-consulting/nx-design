import { createIcon } from '../_createIcon/createIcon';

import IconFileUploadSizeAll from './IconFileUpload_size_all';

export const IconFileUpload = createIcon({
  l: IconFileUploadSizeAll,
  m: IconFileUploadSizeAll,
  s: IconFileUploadSizeAll,
  xs: IconFileUploadSizeAll,
  name: 'IconFileUpload',
});
