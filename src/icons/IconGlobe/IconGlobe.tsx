import { createIcon } from '../_createIcon/createIcon';

import IconGlobeSizeAll from './IconGlobe_size_all';

export const IconGlobe = createIcon({
  l: IconGlobeSizeAll,
  m: IconGlobeSizeAll,
  s: IconGlobeSizeAll,
  xs: IconGlobeSizeAll,
  name: 'IconGlobe',
});
