import { createIcon } from '../_createIcon/createIcon';

import TextAreaSizeAll from './TextArea_size_all';

export const TextArea = createIcon({
  l: TextAreaSizeAll,
  m: TextAreaSizeAll,
  s: TextAreaSizeAll,
  xs: TextAreaSizeAll,
  name: 'IconTextArea',
});
