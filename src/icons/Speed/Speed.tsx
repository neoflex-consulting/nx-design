import { createIcon } from '../_createIcon/createIcon';

import SpeedSizeAll from './Speed_size_all';

export const Speed = createIcon({
  l: SpeedSizeAll,
  m: SpeedSizeAll,
  s: SpeedSizeAll,
  xs: SpeedSizeAll,
  name: 'Speed',
});
