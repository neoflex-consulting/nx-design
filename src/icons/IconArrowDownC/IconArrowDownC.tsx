import { createIcon } from '../_createIcon/createIcon';

import IconArrowDownCSizeAll from './IconArrowDownC_size_all';

export const IconArrowDownC = createIcon({
  l: IconArrowDownCSizeAll,
  m: IconArrowDownCSizeAll,
  s: IconArrowDownCSizeAll,
  xs: IconArrowDownCSizeAll,
  name: 'IconArrowDownC'
});
