import { createIcon } from '../_createIcon/createIcon';

import ShareSizeAll from './Share_size_all';

export const Share = createIcon({
  l: ShareSizeAll,
  m: ShareSizeAll,
  s: ShareSizeAll,
  xs: ShareSizeAll,
  name: 'IconShare',
});
