import { createIcon } from '../_createIcon/createIcon';

import ChatHelpSizeAll from './ChatHelp_size_all';

export const ChatHelp = createIcon({
  l: ChatHelpSizeAll,
  m: ChatHelpSizeAll,
  s: ChatHelpSizeAll,
  xs: ChatHelpSizeAll,
  name: 'ChatHelp',
});
