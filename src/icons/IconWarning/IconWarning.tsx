import { createIcon } from '../_createIcon/createIcon';

import IconWarningSizeAll from './IconWarning_size_all';

export const IconWarning = createIcon({
  l: IconWarningSizeAll,
  m: IconWarningSizeAll,
  s: IconWarningSizeAll,
  xs: IconWarningSizeAll,
  name: 'IconWarning',
});
