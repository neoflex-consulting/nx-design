import { createIcon } from '../_createIcon/createIcon';

import IconDirectorySizeAll from './IconDirectory_size_all';

export const IconDirectory = createIcon({
  l: IconDirectorySizeAll,
  m: IconDirectorySizeAll,
  s: IconDirectorySizeAll,
  xs: IconDirectorySizeAll,
  name: 'IconDirectory',
});
