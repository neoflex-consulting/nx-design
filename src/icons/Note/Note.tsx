import { createIcon } from '../_createIcon/createIcon';

import IconNoteSizeAll from './IconNote_size_all';

export const IconNote = createIcon({
  l: IconNoteSizeAll,
  m: IconNoteSizeAll,
  s: IconNoteSizeAll,
  xs: IconNoteSizeAll,
  name: 'IconNote',
});
