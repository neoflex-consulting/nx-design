import { createIcon } from '../_createIcon/createIcon';

import IconComposeSizeAll from './IconCompose_size_all';

export const IconCompose = createIcon({
  l: IconComposeSizeAll,
  m: IconComposeSizeAll,
  s: IconComposeSizeAll,
  xs: IconComposeSizeAll,
  name: 'IconCompose',
});
