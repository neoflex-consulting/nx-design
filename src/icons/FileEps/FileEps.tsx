import { createIcon } from '../_createIcon/createIcon';

import FileEpsSizeAll from './FileEps_size_all';

export const FileEps = createIcon({
  l: FileEpsSizeAll,
  m: FileEpsSizeAll,
  s: FileEpsSizeAll,
  xs: FileEpsSizeAll,
  name: 'IconFileEps',
});
