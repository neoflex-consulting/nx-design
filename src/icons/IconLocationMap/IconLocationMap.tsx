import { createIcon } from '../_createIcon/createIcon';

import IconLocationMapSizeAll from './IconLocationMap_size_all';

export const IconLocationMap = createIcon({
  l: IconLocationMapSizeAll,
  m: IconLocationMapSizeAll,
  s: IconLocationMapSizeAll,
  xs: IconLocationMapSizeAll,
  name: 'IconLocationMap',
});
