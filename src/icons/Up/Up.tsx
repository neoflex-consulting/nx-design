import { createIcon } from '../_createIcon/createIcon';

import UpSizeAll from './Up_size_all';

export const Up = createIcon({
  l: UpSizeAll,
  m: UpSizeAll,
  s: UpSizeAll,
  xs: UpSizeAll,
  name: 'Up',
});
