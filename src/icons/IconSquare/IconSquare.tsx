import { createIcon } from '../_createIcon/createIcon';

import IconSquareSizeAll from './IconSquare_size_all';

export const IconSquare = createIcon({
  l: IconSquareSizeAll,
  m: IconSquareSizeAll,
  s: IconSquareSizeAll,
  xs: IconSquareSizeAll,
  name: 'IconSquare',
});
