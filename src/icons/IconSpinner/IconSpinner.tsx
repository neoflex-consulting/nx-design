import { createIcon } from '../_createIcon/createIcon';

import IconSpinnerSizeAll from './IconSpinner_size_all';

export const IconSpinner = createIcon({
  l: IconSpinnerSizeAll,
  m: IconSpinnerSizeAll,
  s: IconSpinnerSizeAll,
  xs: IconSpinnerSizeAll,
  name: 'IconSpinner',
});
