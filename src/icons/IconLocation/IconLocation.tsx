import { createIcon } from '../_createIcon/createIcon';

import IconLocationSizeAll from './IconLocation_size_all';

export const IconLocation = createIcon({
  l: IconLocationSizeAll,
  m: IconLocationSizeAll,
  s: IconLocationSizeAll,
  xs: IconLocationSizeAll,
  name: 'IconLocation',
});
