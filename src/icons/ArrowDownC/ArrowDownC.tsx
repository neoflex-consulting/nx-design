import { createIcon } from '../_createIcon/createIcon';

import ArrowDownCSizeAll from './ArrowDownC_size_all';

export const ArrowDownC = createIcon({
  m: ArrowDownCSizeAll,
  s: ArrowDownCSizeAll,
  xs: ArrowDownCSizeAll,
  name: 'ArrowDownC',
});
