import { createIcon } from '../_createIcon/createIcon';

import IconMinusSCheckSizeAll from './IconMinusSCheck_size_all';

export const IconMinusSCheck = createIcon({
  l: IconMinusSCheckSizeAll,
  m: IconMinusSCheckSizeAll,
  s: IconMinusSCheckSizeAll,
  xs: IconMinusSCheckSizeAll,
  name: 'IconMinusSCheck',
});
