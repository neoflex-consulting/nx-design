import { createIcon } from '../_createIcon/createIcon';

import GroupSizeAll from './Group_size_all';

export const Group = createIcon({
  l: GroupSizeAll,
  m: GroupSizeAll,
  s: GroupSizeAll,
  xs: GroupSizeAll,
  name: 'Group',
});
