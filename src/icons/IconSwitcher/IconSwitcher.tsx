import { createIcon } from '../_createIcon/createIcon';

import IconSwitcherSizeAll from './IconSwitcher_size_all';

export const IconSwitcher = createIcon({
  l: IconSwitcherSizeAll,
  m: IconSwitcherSizeAll,
  s: IconSwitcherSizeAll,
  xs: IconSwitcherSizeAll,
  name: 'IconSwitcher',
});
