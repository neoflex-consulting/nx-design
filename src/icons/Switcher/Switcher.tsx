import { createIcon } from '../_createIcon/createIcon';

import SwitcherSizeAll from './Switcher_size_all';

export const Switcher = createIcon({
  l: SwitcherSizeAll,
  m: SwitcherSizeAll,
  s: SwitcherSizeAll,
  xs: SwitcherSizeAll,
  name: 'IconSwitcher',
});
