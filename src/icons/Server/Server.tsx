import { createIcon } from '../_createIcon/createIcon';

import ServerSizeAll from './Server_size_all';

export const Server = createIcon({
  m: ServerSizeAll,
  s: ServerSizeAll,
  xs: ServerSizeAll,
  name: 'Server',
});
