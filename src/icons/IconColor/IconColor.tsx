import { createIcon } from '../_createIcon/createIcon';

import IconColorSizeAll from './IconColor_size_all';

export const IconColor = createIcon({
  l: IconColorSizeAll,
  m: IconColorSizeAll,
  s: IconColorSizeAll,
  xs: IconColorSizeAll,
  name: 'IconColor',
});
