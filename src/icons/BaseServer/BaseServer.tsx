import { createIcon } from '../createIcon/createIcon';

import BaseServerSizeAll from './BaseServer_size_all';

export const BaseServer = createIcon({
  m: BaseServerSizeAll,
  s: BaseServerSizeAll,
  xs: BaseServerSizeAll,
  name: 'BaseServer',
});
