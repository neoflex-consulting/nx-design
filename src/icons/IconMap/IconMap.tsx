import { createIcon } from '../_createIcon/createIcon';

import IconMapSizeAll from './IconMap_size_all';

export const IconMap = createIcon({
  l: IconMapSizeAll,
  m: IconMapSizeAll,
  s: IconMapSizeAll,
  xs: IconMapSizeAll,
  name: 'IconMap',
});
