import { createIcon } from '../_createIcon/createIcon';

import IconKeySizeAll from './IconKey_size_all';

export const IconKey = createIcon({
  l: IconKeySizeAll,
  m: IconKeySizeAll,
  s: IconKeySizeAll,
  xs: IconKeySizeAll,
  name: 'IconKey',
});
