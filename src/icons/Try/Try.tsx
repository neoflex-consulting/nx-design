import { createIcon } from '../_createIcon/createIcon';

import TrySizeAll from './Try_size_all';

export const Try = createIcon({
  l: TrySizeAll,
  m: TrySizeAll,
  s: TrySizeAll,
  xs: TrySizeAll,
  name: 'IconTry',
});
