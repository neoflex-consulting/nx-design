import { createIcon } from '../_createIcon/createIcon';

import IconBreifcaseSizeAll from './IconBreifcase_size_all';

export const IconBreifcase = createIcon({
  l: IconBreifcaseSizeAll,
  m: IconBreifcaseSizeAll,
  s: IconBreifcaseSizeAll,
  xs: IconBreifcaseSizeAll,
  name: 'IconBreifcase',
});
