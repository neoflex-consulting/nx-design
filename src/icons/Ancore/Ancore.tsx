import { createIcon } from '../_createIcon/createIcon';

import AncoreSizeAll from './Ancore_size_all';

export const Ancore = createIcon({
  m: AncoreSizeAll,
  s: AncoreSizeAll,
  xs: AncoreSizeAll,
  name: 'Alert',
});
