import { createIcon } from '../_createIcon/createIcon';

import WifiSizeAll from './Wifi_size_all';

export const Wifi = createIcon({
  m: WifiSizeAll,
  s: WifiSizeAll,
  xs: WifiSizeAll,
  name: 'Wifi',
});
