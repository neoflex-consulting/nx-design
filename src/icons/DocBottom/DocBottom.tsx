import { createIcon } from '../_createIcon/createIcon';

import DocBottomSizeAll from './DocBottom_size_all';

export const DocBottom = createIcon({
  l: DocBottomSizeAll,
  m: DocBottomSizeAll,
  s: DocBottomSizeAll,
  xs: DocBottomSizeAll,
  name: 'DocBottom',
});
