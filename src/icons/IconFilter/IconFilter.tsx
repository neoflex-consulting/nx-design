import { createIcon } from '../_createIcon/createIcon';

import IconFilterSizeAll from './IconFilter_size_all';

export const IconFilter = createIcon({
  l: IconFilterSizeAll,
  m: IconFilterSizeAll,
  s: IconFilterSizeAll,
  xs: IconFilterSizeAll,
  name: 'IconFilter',
});
