import { createIcon } from '../_createIcon/createIcon';

import BellSizeAll from './Bell_size_all';

export const Bell = createIcon({
  l: BellSizeAll,
  m: BellSizeAll,
  s: BellSizeAll,
  xs: BellSizeAll,
  name: 'IconBell',
});
