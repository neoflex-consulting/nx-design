import { createIcon } from '../_createIcon/createIcon';

import IconFilesSizeAll from './IconFiles_size_all';

export const IconFiles = createIcon({
  l: IconFilesSizeAll,
  m: IconFilesSizeAll,
  s: IconFilesSizeAll,
  xs: IconFilesSizeAll,
  name: 'IconFiles',
});
