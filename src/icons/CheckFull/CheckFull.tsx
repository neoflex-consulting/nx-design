import { createIcon } from '../_createIcon/createIcon';

import CheckFullSizeAll from './CheckFull_size_all';

export const CheckFull = createIcon({
  l: CheckFullSizeAll,
  m: CheckFullSizeAll,
  s: CheckFullSizeAll,
  xs: CheckFullSizeAll,
  name: 'IconCheckFull',
});
