import { createIcon } from '../_createIcon/createIcon';

import IconPlusCSizeAll from './IconPlusC_size_all';

export const IconPlusC = createIcon({
  l: IconPlusCSizeAll,
  m: IconPlusCSizeAll,
  s: IconPlusCSizeAll,
  xs: IconPlusCSizeAll,
  name: 'IconPlusC',
});
