import { createIcon } from '../_createIcon/createIcon';

import UserAddSizeAll from './UserAdd_size_all';

export const UserAdd = createIcon({
  l: UserAddSizeAll,
  m: UserAddSizeAll,
  s: UserAddSizeAll,
  xs: UserAddSizeAll,
  name: 'UserAdd',
});
