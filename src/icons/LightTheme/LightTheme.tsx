import { createIcon } from '../_createIcon/createIcon';

import LightThemeSizeAll from './LightTheme_size_all';

export const LightTheme = createIcon({
  l: LightThemeSizeAll,
  m: LightThemeSizeAll,
  s: LightThemeSizeAll,
  xs: LightThemeSizeAll,
  name: 'IconLightTheme',
});
