import { createIcon } from '../_createIcon/createIcon';

import CloseCSizeAll from './CloseC_size_all';

export const CloseC = createIcon({
  m: CloseCSizeAll,
  s: CloseCSizeAll,
  xs: CloseCSizeAll,
  name: 'CloseC',
});
