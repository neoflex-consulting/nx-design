import { createIcon } from '../_createIcon/createIcon';

import KeySizeAll from './Key_size_all';

export const Key = createIcon({
  l: KeySizeAll,
  m: KeySizeAll,
  s: KeySizeAll,
  xs: KeySizeAll,
  name: 'Key',
});
