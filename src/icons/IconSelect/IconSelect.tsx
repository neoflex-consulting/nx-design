import { createIcon } from '../_createIcon/createIcon';

import IconSelectSizeAll from './IconSelect_size_all';

export const IconSelect = createIcon({
  l: IconSelectSizeAll,
  m: IconSelectSizeAll,
  s: IconSelectSizeAll,
  xs: IconSelectSizeAll,
  name: 'IconSelect',
});
