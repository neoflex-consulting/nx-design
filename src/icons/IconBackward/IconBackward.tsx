import { createIcon } from '../_createIcon/createIcon';

import IconBackwardSizeAll from './IconBackward_size_all';

export const IconBackward = createIcon({
  l: IconBackwardSizeAll,
  m: IconBackwardSizeAll,
  s: IconBackwardSizeAll,
  xs: IconBackwardSizeAll,
  name: 'IconBackward',
});
