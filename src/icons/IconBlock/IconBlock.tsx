import { createIcon } from '../_createIcon/createIcon';

import IconBlockSizeAll from './IconBlock_size_all';

export const IconBlock = createIcon({
  l: IconBlockSizeAll,
  m: IconBlockSizeAll,
  s: IconBlockSizeAll,
  xs: IconBlockSizeAll,
  name: 'IconBlock',
});
