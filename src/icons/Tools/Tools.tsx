import { createIcon } from '../_createIcon/createIcon';

import ToolsSizeAll from './Tools_size_all';

export const Tools = createIcon({
  l: ToolsSizeAll,
  m: ToolsSizeAll,
  s: ToolsSizeAll,
  xs: ToolsSizeAll,
  name: 'IconTools',
});
