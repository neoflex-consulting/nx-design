import { createIcon } from '../_createIcon/createIcon';

import ChatDialogSizeAll from './ChatDialog_size_all';

export const ChatDialog = createIcon({
  l: ChatDialogSizeAll,
  m: ChatDialogSizeAll,
  s: ChatDialogSizeAll,
  xs: ChatDialogSizeAll,
  name: 'ChatDialog',
});
