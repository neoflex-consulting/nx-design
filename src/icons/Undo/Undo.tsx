import { createIcon } from '../_createIcon/createIcon';

import UndoSizeAll from './Undo_size_all';

export const Undo = createIcon({
  l: UndoSizeAll,
  m: UndoSizeAll,
  s: UndoSizeAll,
  xs: UndoSizeAll,
  name: 'IconUndo',
});
