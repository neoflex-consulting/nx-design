import { createIcon } from '../_createIcon/createIcon';

import QcodeSizeAll from './Qcode_size_all';

export const Qcode = createIcon({
  l: QcodeSizeAll,
  m: QcodeSizeAll,
  s: QcodeSizeAll,
  xs: QcodeSizeAll,
  name: 'IconQcode',
});
