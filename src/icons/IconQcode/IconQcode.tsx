import { createIcon } from '../_createIcon/createIcon';

import IconQcodeSizeAll from './IconQcode_size_all';

export const IconQcode = createIcon({
  l: IconQcodeSizeAll,
  m: IconQcodeSizeAll,
  s: IconQcodeSizeAll,
  xs: IconQcodeSizeAll,
  name: 'IconQcode',
});
