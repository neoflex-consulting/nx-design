import { createIcon } from '../_createIcon/createIcon';

import CaretDownSizeAll from './CaretDown_size_all';

export const CaretDown = createIcon({
  m: CaretDownSizeAll,
  s: CaretDownSizeAll,
  xs: CaretDownSizeAll,
  name: 'CaretDown',
});
