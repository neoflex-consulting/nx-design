import { createIcon } from '../_createIcon/createIcon';

import IconAlertSizeAll from './IconAlert_size_all';

export const IconAlert = createIcon({
  l: IconAlertSizeAll,
  m: IconAlertSizeAll,
  s: IconAlertSizeAll,
  xs: IconAlertSizeAll,
  name: 'IconAlert'
});
