import { createIcon } from '../_createIcon/createIcon';

import IconCodeSizeAll from './IconCode_size_all';

export const IconCode = createIcon({
  l: IconCodeSizeAll,
  m: IconCodeSizeAll,
  s: IconCodeSizeAll,
  xs: IconCodeSizeAll,
  name: 'IconCode',
});
