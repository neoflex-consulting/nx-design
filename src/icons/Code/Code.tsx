import { createIcon } from '../_createIcon/createIcon';

import CodeSizeAll from './Code_size_all';

export const Code = createIcon({
  l: CodeSizeAll
  m: CodeSizeAll,
  s: CodeSizeAll,
  xs: CodeSizeAll,
  name: 'Code',
});
