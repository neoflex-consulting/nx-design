import { createIcon } from '../_createIcon/createIcon';

import FileDeleteSizeAll from './FileDelete_size_all';

export const FileDelete = createIcon({
  l: FileDeleteSizeAll,
  m: FileDeleteSizeAll,
  s: FileDeleteSizeAll,
  xs: FileDeleteSizeAll,
  name: 'FileDelete',
});
