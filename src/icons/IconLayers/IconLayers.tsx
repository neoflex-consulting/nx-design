import { createIcon } from '../_createIcon/createIcon';

import IconLayersSizeAll from './IconLayers_size_all';

export const IconLayers = createIcon({
  l: IconLayersSizeAll,
  m: IconLayersSizeAll,
  s: IconLayersSizeAll,
  xs: IconLayersSizeAll,
  name: 'IconLayers',
});
