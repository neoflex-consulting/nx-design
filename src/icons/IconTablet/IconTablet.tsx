import { createIcon } from '../_createIcon/createIcon';

import IconTabletSizeAll from './IconTablet_size_all';

export const IconTablet = createIcon({
  l: IconTabletSizeAll,
  m: IconTabletSizeAll,
  s: IconTabletSizeAll,
  xs: IconTabletSizeAll,
  name: 'IconTablet',
});
