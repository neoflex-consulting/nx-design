import { createIcon } from '../_createIcon/createIcon';

import EyeSizeAll from './Eye_size_all';

export const Eye = createIcon({
  l: EyeSizeAll,
  m: EyeSizeAll,
  s: EyeSizeAll,
  xs: EyeSizeAll,
  name: 'IconEye',
});
