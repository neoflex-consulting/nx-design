import { createIcon } from '../_createIcon/createIcon';

import FilePdfSizeAll from './FilePdf_size_all';

export const FilePdf = createIcon({
  m: FilePdfSizeAll,
  s: FilePdfSizeAll,
  xs: FilePdfSizeAll,
  name: 'FilePdf',
});
