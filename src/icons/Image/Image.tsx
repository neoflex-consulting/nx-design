import { createIcon } from '../_createIcon/createIcon';

import ImageSizeAll from './Image_size_all';

export const Image = createIcon({
  l: ImageSizeAll,
  m: ImageSizeAll,
  s: ImageSizeAll,
  xs: ImageSizeAll,
  name: 'Image',
});
