import { createIcon } from '../_createIcon/createIcon';

import FortSizeAll from './Fort_size_all';

export const Fort = createIcon({
  m: FortSizeAll,
  s: FortSizeAll,
  xs: FortSizeAll,
  name: 'Fort',
});
