import { createIcon } from '../_createIcon/createIcon';

import IconServerSizeAll from './IconServer_size_all';

export const IconServer = createIcon({
  l: IconServerSizeAll,
  m: IconServerSizeAll,
  s: IconServerSizeAll,
  xs: IconServerSizeAll,
  name: 'IconServer',
});
