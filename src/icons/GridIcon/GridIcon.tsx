import { createIcon } from '../_createIcon/createIcon';

import GridIconSizeAll from './GridIcon_size_all';

export const GridIcon = createIcon({
  l: GridIconSizeAll,
  m: GridIconSizeAll,
  s: GridIconSizeAll,
  xs: GridIconSizeAll,
  name: 'GridIcon',
});
