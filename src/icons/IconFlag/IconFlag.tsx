import { createIcon } from '../_createIcon/createIcon';

import IconFlagSizeAll from './IconFlag_size_all';

export const IconFlag = createIcon({
  l: IconFlagSizeAll,
  m: IconFlagSizeAll,
  s: IconFlagSizeAll,
  xs: IconFlagSizeAll,
  name: 'IconFlag',
});
