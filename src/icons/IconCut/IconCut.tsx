import { createIcon } from '../_createIcon/createIcon';

import IconCutSizeAll from './IconCut_size_all';

export const IconCut = createIcon({
  l: IconCutSizeAll,
  m: IconCutSizeAll,
  s: IconCutSizeAll,
  xs: IconCutSizeAll,
  name: 'IconCut',
});
