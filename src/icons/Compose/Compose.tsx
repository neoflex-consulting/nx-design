import { createIcon } from '../_createIcon/createIcon';

import ComposeSizeAll from './Compose_size_all';

export const Compose = createIcon({
  l: ComposeSizeAll,
  m: ComposeSizeAll,
  s: ComposeSizeAll,
  xs: ComposeSizeAll,
  name: 'IconCompose',
});
