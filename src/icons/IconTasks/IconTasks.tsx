import { createIcon } from '../_createIcon/createIcon';

import IconTasksSizeAll from './IconTasks_size_all';

export const IconTasks = createIcon({
  l: IconTasksSizeAll,
  m: IconTasksSizeAll,
  s: IconTasksSizeAll,
  xs: IconTasksSizeAll,
  name: 'IconTasks',
});
