import { createIcon } from '../_createIcon/createIcon';

import SearchInSizeAll from './SearchIn_size_all';

export const SearchIn = createIcon({
  m: SearchInSizeAll,
  s: SearchInSizeAll,
  xs: SearchInSizeAll,
  name: 'SearchIn',
});
