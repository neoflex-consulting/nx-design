import { createIcon } from '../createIcon/createIcon';

import BookUploadSizeAll from './BookUpload_size_all';

export const BookUpload = createIcon({
  m: BookUploadSizeAll,
  s: BookUploadSizeAll,
  xs: BookUploadSizeAll,
  name: 'BookUpload',
});
