import { createIcon } from '../_createIcon/createIcon';

import UngroupSizeAll from './Ungroup_size_all';

export const Ungroup = createIcon({
  l: UngroupSizeAll,
  m: UngroupSizeAll,
  s: UngroupSizeAll,
  xs: UngroupSizeAll,
  name: 'IconUngroup',
});
