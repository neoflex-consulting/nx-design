import { createIcon } from '../_createIcon/createIcon';

import IconFilePngSizeAll from './IconFilePng_size_all';

export const IconFilePng = createIcon({
  l: IconFilePngSizeAll,
  m: IconFilePngSizeAll,
  s: IconFilePngSizeAll,
  xs: IconFilePngSizeAll,
  name: 'IconFilePng',
});
