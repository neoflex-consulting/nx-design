import { createIcon } from '../_createIcon/createIcon';

import PlayFilledSizeAll from './PlayFilled_size_all';

export const PlayFilled = createIcon({
  m: PlayFilledSizeAll,
  s: PlayFilledSizeAll,
  xs: PlayFilledSizeAll,
  name: 'PlayFilled',
});
