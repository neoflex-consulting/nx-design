import { createIcon } from '../_createIcon/createIcon';

import TablePivotSizeAll from './TablePivot_size_all';

export const TablePivot = createIcon({
  l: TablePivotSizeAll,
  m: TablePivotSizeAll,
  s: TablePivotSizeAll,
  xs: TablePivotSizeAll,
  name: 'IconTablePivot',
});
