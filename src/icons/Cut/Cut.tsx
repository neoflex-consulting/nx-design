import { createIcon } from '../_createIcon/createIcon';

import CutSizeAll from './Cut_size_all';

export const Cut = createIcon({
  l: CutSizeAll,
  m: CutSizeAll,
  s: CutSizeAll,
  xs: CutSizeAll,
  name: 'IconCut',
});
