import { createIcon } from '../_createIcon/createIcon';

import IconFileEpsSizeAll from './IconFileEps_size_all';

export const IconFileEps = createIcon({
  l: IconFileEpsSizeAll,
  m: IconFileEpsSizeAll,
  s: IconFileEpsSizeAll,
  xs: IconFileEpsSizeAll,
  name: 'IconFileEps',
});
