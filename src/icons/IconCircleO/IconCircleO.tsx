import { createIcon } from '../_createIcon/createIcon';

import IconCircleOSizeAll from './IconCircleO_size_all';

export const IconCircleO = createIcon({
  l: IconCircleOSizeAll,
  m: IconCircleOSizeAll,
  s: IconCircleOSizeAll,
  xs: IconCircleOSizeAll,
  name: 'IconCircleO',
});
