import { createIcon } from '../_createIcon/createIcon';

import UsersSizeAll from './Users_size_all';

export const Users = createIcon({
  m: UsersSizeAll,
  s: UsersSizeAll,
  xs: UsersSizeAll,
  name: 'Users',
});
