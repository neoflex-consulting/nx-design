import { createIcon } from '../_createIcon/createIcon';

import IconCheckCSizeAll from './IconCheckC_size_all';

export const IconCheckC = createIcon({
  l: IconCheckCSizeAll,
  m: IconCheckCSizeAll,
  s: IconCheckCSizeAll,
  xs: IconCheckCSizeAll,
  name: 'IconCheckC',
});
