import { createIcon } from '../_createIcon/createIcon';

import IconFlashSizeAll from './IconFlash_size_all';

export const IconFlash = createIcon({
  l: IconFlashSizeAll,
  m: IconFlashSizeAll,
  s: IconFlashSizeAll,
  xs: IconFlashSizeAll,
  name: 'IconFlash',
});
