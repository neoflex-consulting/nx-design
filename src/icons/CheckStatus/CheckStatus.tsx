import { createIcon } from '../_createIcon/createIcon';

import CheckStatusSizeAll from './CheckStatus_size_all';

export const CheckStatus = createIcon({
  m: CheckStatusSizeAll,
  s: CheckStatusSizeAll,
  xs: CheckStatusSizeAll,
  name: 'CheckStatus',
});
