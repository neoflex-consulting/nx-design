import { AlertExampleView } from './examples/AlertExampleView/AlertExampleView';
import { AlertExampleType } from './examples/AlertExampleType/AlertExampleType';
import { AlertExampleIcon } from './examples/AlertExampleIcon/AlertExampleIcon';
import { AlertExampleSuccess } from './examples/AlertExampleText/AlertExampleText';
import { AlertExampleAlert } from './examples/AlertExampleText/AlertExampleText';
import { AlertExampleSize } from './examples/AlertExampleSize/AlertExampleSize';

# Alert

Нужен, чтобы показывать пользователям сообщения. Например, об ошибке в заполненной форме
или о том, что изменения сохранились.

- [Тип сообщения](#тип-сообщения)
- [Текст сообщения](#текст-сообщения)
  - [Как писать сообщения](#как-писать-сообщения)
  - [Когда нужен заголовок](#когда-нужен-заголовок)
- [Внешний вид](#внешний-вид)
  - [Типизация](#типизация)
  - [Иконка](#иконка)
  - [Размер](#размер)
- [Список свойств](#свойства)
- [Пример использования](#пример)

## Тип сообщения

За тип сообщения отвечает свойство `type`.

<AlertExampleType />

## Текст сообщения

Текст состоит из двух частей:

- заголовок задаётся свойством `title`,
- основной текст — свойством `lable`.

Вы можете написать сообщение без заголовка. А вот заголовок без текста лучше не использовать:
читателю может показаться, что на него кричат.

### Как писать сообщения

Как можно короче — только самое важное. Всегда с прописной буквы. Если в сообщении одно предложение,
в конце точка не нужна. Если предложений несколько или есть заголовок, можно поставить точку (но не обязательно).
Главное делать одинаково: или везде ставить точки, или нигде не ставить.

### Когда нужен заголовок

Если есть важная информация, к которой нужно сразу привлечь внимание. А к ней — пояснения. Например, так:

<AlertExampleSuccess />

```tsx
<Alert
  status="success"
  view="filled"
  title="Заявка отправлена"
  label="Подождите до завтра: мы всё посчитаем и вам позвоним"
/>
```

Или так:

<AlertExampleAlert />

```tsx
<Alert
  status="error"
  view="filled"
  title="Ничего не вышло"
  label="Попробуйте выйти и зайти снова. Если не получается, нажмите красную кнопку три раза"
/>
```

Если всё важное можно передать одним коротким предложением, лучше обойтись без заголовка.

## Внешний вид

### Типизация

За тип alert отвечает свойство `view`. В дизайн-системе есть alert трех типов:
с заливкой `filled`, без неё `bordered` и с обводкой `outlined`.

<AlertExampleView />

```tsx
<Alert label="Ваше сообщение отправлено" view="filled" status="success" />
```

```tsx
<Alert label="Всё заполнено" view="bordered" status="success" />
```

```tsx
<Alert label="Всё заполнено" view="outlined" status="success" />
```

### Иконка

С помощью свойства `icon` можно добавить иконку — любую из [библиотеки иконок](?path=/story/components-icons--playground).

Не стоит добавлять иконку в alert без заливки.

<AlertExampleIcon />

```tsx
<Alert label="Сообщение отправлено" view="filled" status="success" icon={IconThumbUp}/>
```

### Размер

За размер сообщения отвечает свойство `size`. Варианты: `l`, `m`, `s`, по умолчанию — `s`.

<AlertExampleSize />

## Свойства

| Свойство                     | Тип или варианты значения                       | По умолчанию | Описание                           |
| ---------------------------- | ------------------------------------------------------------ | ------------ | ---------------------------------- |
| [`status?`](#тип-сообщения)  | `'system'`, `'error'`, `'warning'`, '`success'`, '`info'`    | `'success'`  | Тип сообщения                      |
| [`title?`](#текст-сообщения) | `string`                                                     | —            | Заголовок сообщения                |
| [`lable?`](#текст-сообщения) | `string`                                                     | —            | Текст сообщения                    |
| `children?`                  | `React.ReactNode`                                            | —            |                                    |
| [`view?`](#внешний-вид)      | `'filled'`, `'bordered'`, `'outlined'`                       | `'filled'`   | Тип alert (с заливкой или без)     |
| [`icon?`](#внешний-вид)      | `React.FC<IconProps>`                                        | —            | Иконка                             |
| `size?`                      | `'s'`, `'m'`, `'l'`                                          | `'s'`        | Размер                             |
| `ref?`                       | `React.Ref<HTMLDivElement>`                                  | —            | Ссылка на DOM-элемент компонента   |
| `className`                  | `string`                                                     | —            | Дополнительный CSS-класс           |

## Пример

```ts
import React from 'react';
import { Alert } from 'nx-design/Alert';

const App = () => {
  return <Alert title="Всё хорошо" label="Уже сейчас всё хорошо, а будет ещё лучше" />;
};
```
